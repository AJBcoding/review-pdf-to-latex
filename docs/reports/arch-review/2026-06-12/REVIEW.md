# Architectural Review — review-pdf-to-latex (2026-06-12)

## Executive Summary

**What was reviewed, and how much to trust it.** Six serial passes (inventory → engine → desktop main → renderer → multi-format capability question → synthesis) over the two-layer review-pdf-to-latex system: a ~6.5K-LOC Python CLI engine and an ~18.9K-LOC Electron desktop app, by a solo developer in ~27 days. Every finding below carries re-openable citations. In this pass, the 14 highest-impact claims (C1–C14) were each re-verified by an independent read-only verifier that re-opened the cited lines or re-ran the cited commands: **14/14 CONFIRMED — zero WRONG, zero UNVERIFIABLE.** Nothing was deleted or downgraded; confirmed claims carry a trailing ✓ in their sections. Two items strengthened under verification: the one uncited link in the review (git's behavior staging ignored paths, C2) was reproduced on git 2.50.1, and the one flag carried unverified since Pass 3 (first-window IPC binding, C13) was confirmed as a real silent-event-drop bug. The unverified remainder (~45 further findings) came from the same lane process plus per-pass synthesizer spot-checks, which caught and corrected three counting errors along the way (PROGRESS.md) — calibrate accordingly: high confidence, not 100% sampled.

**Overall verdict.** The foundation is genuinely strong for a 27-day solo project — 354 engine tests over 13/14 modules, atomic-write discipline, strict TypeScript, clean dependencies, dated specs and research docs. But correctness debt clusters in exactly the two places the charter says matter most: the daily-driver paths and the comment/anchor data model.

**1. Five shipping data-loss / self-defeating bugs, all S-sized fixes.** The first keystroke in any frontmatter'd .md permanently deletes its YAML block from disk — in the app's only editable format (C7 ✓). Re-opening a v1 doc after a completed round re-seeds the v1.1 drafts sidecar wholesale, destroying comments added there since (C8 ✓). Clicking Abandon resurrects the abandoned round — neither the watcher nor the renderer filters the `.abandoned.json` tombstone (C9 ✓). Engine-side, `apply_edit`/`revert_edit` mutate the .tex file before status validation, leaving unrecorded mutations on failure (C1 ✓), and `commit-phase` stages `.gitignore`'d state files without `-f`, so it fails with exit 19 on every extract-bootstrapped project (C2 ✓, repro'd). Related: every submit unhappy path strands the user — timeout Re-sling is a guaranteed no-op, Resume is circular, Retry violates its own same-submit_id contract, and failed slings leave comments `submitted` forever (C10 ✓).

**2. The anchor model is the central architectural seam — and it is a three-party schema event.** Every comment in every format carries a REQUIRED PDF-shaped anchor; the format discriminator is file-level with two declared kinds covering four real shapes; HTML/DOCX anchors are smuggled through `md_anchor` with `as any` (C6 ✓); the PDF bundle writer consumes the anchor blind (C5 ✓); and the rig's `new_anchor` relocation field is PDF-typed too (C12 ✓) — so the per-comment discriminated-union restructure, named by three passes as the highest-leverage pre-work for the unified comment model, must be coordinated across drafts sidecar, submit/results files, AND the rig contract.

**3. Duplication has become behavioral divergence.** The two live Claude routes differ on permissions (skip-permissions default vs interactive approval), cwd, worker caps, and resume (C11 ✓) — and on the new-pane route, Create Context/Sling workers are fire-and-forget with no visible UI; the flag flip is not yet safe as a default. The "typed" IPC surface is vacuous on the result side and has already drifted on both bridges; four cloned doc-loaders' reset lists have diverged; the iframe viewers are drifted twins; the atomic-write helper exists 5×; the engine exit-code contract exists in four notations with a live `21` overload.

**4. Migration safety is fictional on the hot path.** Every state-mutating read (apply, commit, server) bypasses the schema-version guard, and the CLI catches neither schema exception (C3 ✓) — this must be fixed before the schema-v2 bump the unified model forces. The legacy HTTP viewer has demonstrably rotted (its auto-reload contract is unimplemented, so the core loop can never fire — C4 ✓); its reusable flock-disciplined event bus should be split out before a keep-vs-kill decision.

**5. The capability direction is well-grounded in shipped assets.** PDF round-trip splits across libraries already in package.json (pdf.js reads in the existing `renderPage`, pdf-lib writes by extending bundle.ts — both halves proven in-repo). DOCX: a hand-rolled comments.xml adapter in main, v1 flat comments; the SuperDoc rejection stands on new (architecture, not format) grounds; note that comment create/delete must touch document.xml range markers (C14 ✓). The unified model should be the M-2 discriminated anchor union with `quads[]`, `origin`, and `native_ref`. The hardest non-code problem: three status vocabularies (9/7/5 values) share zero strings, with near-miss names — the mapping table is owner-decision spec work. Test investment is currently inverted (zero coverage on the modules that mutate user files); the in-repo agent-pane tests are the template to copy.

GATE 1 is CLOSED: `## Roadmap` remains a placeholder, and no proposals were written. The GATE 1 Review Packet at the end of this document is the decision aid for opening Phase B.

## Engine — Core Pipeline (extract / apply / state / commit)

**Pass 2 — key takeaways** (covers all three Engine sections)

- Two reachable file-corruption paths sit in the apply/revert hot path — the .tex file is mutated *before* status validation, and reverting an empty-text (deletion) apply silently destroys an unrelated line — both are S-sized fixes (apply.py:300–305 vs :334–338; apply.py:308–313 vs :484–493).
- The migration safety story is fictional on the hot path: every mutating read (apply, commit, server) bypasses `state.read_json`'s schema-version guard, and the CLI catches neither SchemaVersionError nor MigrationRequiredError — this must be fixed before the unified comment model forces the schema-v2 bump (apply.py:121–123; cli.py grep evidence below).
- The legacy HTTP viewer has demonstrably rotted in place — its core click→reload loop cannot fire (no `do_HEAD`, frame.html:666–679 contract unimplemented) and the diff pane is dead in every install — but ~40% of server.py is a reusable, format-agnostic, flock-disciplined event bus that should be split out before any keep-vs-kill decision.
- Contract consistency is the cheapest pre-work for the multi-format direction: the exit-code table exists in four notations with a live `21` overload, two parallel error-mapping conventions, and `--json` honored by only 4 of 17 subcommands — the engine↔desktop seam runs straight through all three.
- The foundation is strong: 354 tests covering 13/14 modules, clean deps with a current lockfile, atomic-write discipline throughout; the core pipeline is closer to format-agnostic than its naming suggests, but the persisted extraction schema drops the subtype/reply data an Acrobat round-trip needs.

---

### apply_edit / revert_edit mutate the .tex file BEFORE status validation — reachable partial-write corruption [high / S]

- FACT: In `apply_edit`, the .tex file is rewritten at apply.py:300–305 (`tex_path.open("w")` + `writelines`), but `validate_status_transition(current_status, "applied", "apply")` runs only afterwards at apply.py:334–338, raising `IllegalStatusTransitionError` before the state.json/mapping.json writes at apply.py:341–342. The transition table has no `("accepted", "apply")`, `("deferred", "apply")`, or `("surfaced_resolved", "apply")` keys (state.py:496–501), so calling `apply_edit` on e.g. an `accepted` annotation raises. `revert_edit` has the same shape: file write at apply.py:487–491, validation at apply.py:512–517, state writes at apply.py:529–530. ✓ (C1, Pass-6 verified)
- JUDGEMENT: On a validation failure the .tex file is already mutated but `before_text` was never persisted and mapping line-shifts were never recorded — the engine has no record the edit happened and cannot revert it. This violates the module's own stated contract (apply.py:4–7).
- RECOMMENDATION: Hoist `validate_status_transition` above the file mutation in both functions (current status is already in hand at apply.py:250/448). Pure reordering, no happy-path behavior change. S.

### commit_phase stages .gitignore'd `.review-state/` files without `-f` — likely exit-19 on every extract-bootstrapped project [high / S]

- FACT: `run_extract` calls `ensure_gitignore_entry(project_dir, entry=".review-state/")` (extract.py:945; default entry extract.py:743–746). `commit_phase` then stages `state.json`, `mapping.json`, `annotations.json`, and `state-events.jsonl` from under `.review-state/` (commit.py:307–314) via `git add -- <paths>` with no `-f` (commit.py:316–327), raising `CommitFailedError` (exit 19) on nonzero return. The commit test fixture `_init_project_repo` (tests/test_commit.py:145–215) never writes a .gitignore, so the interaction is untested. ✓ (C2, Pass-6 verified)
- JUDGEMENT: `git add` on explicitly-named untracked ignored paths exits nonzero ("Use -f if you really want to add them"), so on any real project bootstrapped by `review-pdf extract`, `commit-phase` should fail with exit 19 — extract's gitignore policy and commit's staging policy contradict each other, hidden by the fixture gap. (The git behavior claim was the one uncited link; the Pass-6 verifier reproduced it on git 2.50.1 — `git add` of named ignored paths exits 1 with "Use -f if you really want to add them" — so `commit-phase` fails with exit 19 on extract-bootstrapped projects. Link closed.)
- RECOMMENDATION: Decide which policy wins: stop staging `.review-state/*` (the commit message already points at the snapshot path, commit.py:187) or stage with `git add -f`; add a regression test whose fixture includes the extract-written .gitignore. S.

### Revert after an empty-text apply (deletion) destroys one unrelated line — documented special case nobody handles, untested [med / S]

- FACT: `apply_edit` with empty `new_text` deletes lines and records a degenerate range with the in-code caveat "callers must treat empty edits as a special case" (apply.py:308–313: `map_entry["line_range"] = [start, start]` when `new_lines` is empty). `revert_edit` does not special-case it: it computes `current_count = end - start + 1` (= 1 for the degenerate range) and writes `all_lines[: start - 1] + before_lines + all_lines[end:]` (apply.py:484–485), removing the line now at position `start` — a line that was never part of the original edit — and `line_shift = len(before_lines) - 1` (apply.py:493) under-shifts subsequent mappings by one. No test exercises empty text: `grep -rn 'new_text=""\|empty' tests/test_apply.py` returns nothing; every `new_text=` in tests/test_apply.py is non-empty (tests/test_apply.py:123–605).
- JUDGEMENT: A latent file-corruption bug in the revert path of a legal apply input; both the swallowed line and the off-by-one mapping shift would be silent.
- RECOMMENDATION: Reject empty `new_text` in `apply_edit` (simplest) or encode an explicit empty-range marker special-cased in `revert_edit`; add apply-empty → revert tests. S.

### Half-adopted typed layer: state.py dataclasses are write-only, and the raw-dict mutators have drifted from them [med / M]

- FACT: The dataclasses (~240 LOC of hand-written from_dict/to_dict at state.py:189–430) are constructed only by extract.py's bootstrap path — `grep -rn 'StateFile\|AnnotationState\|MappingCandidate\|Mapping\.from_dict\|Mapping(' src/review_pdf_to_latex/ --include='*.py' | grep -v 'state.py:'` matches only extract.py (imports extract.py:20–23; constructions extract.py:571–733). All mutators operate on raw dicts (e.g. apply.py:322–339, 589–599). Three concrete drifts: (a) `set_annotation_status` writes a `last_status_reason` key (apply.py:598–599) that `AnnotationState` does not define (state.py:313–321; to_dict state.py:343–359 would silently drop it); (b) `override_mapping` sets `entry["candidates"] = None` (apply.py:836) while `Mapping.from_dict` iterates `d.get("candidates", [])` (state.py:275) — TypeError when the key exists as None; (c) `Annotation.created` is typed `str` (state.py:198) but populated from `_format_created -> str | None` (extract.py:28, used at extract.py:363).
- JUDGEMENT: The schema source of truth is split between dataclasses (bootstrap side) and ad-hoc dict keys (mutate side), and they already disagree in three places. The unified cross-format comment model needs a single authoritative schema layer; today there isn't one.
- RECOMMENDATION: Pick a direction before schema-v2: make mutators round-trip through the dataclasses (adding `last_status_reason`, fixing `candidates=None`→`[]`) or demote the dataclasses to bootstrap-only builders and document the dict keys as the schema. Fix drifts (a)–(c) immediately regardless. M (drift fixes alone S).

### Per-operation cost: fuzzy_map re-reads and re-scores the entire .tex tree per annotation; every mutator re-hashes the full source PDF [med / S–M]

- FACT: `fuzzy_map` takes a single annotation and, inside the call, enumerates and reads every .tex file (extract.py:586–588); `run_extract` calls it in a loop over all annotations (extract.py:915–916). For every window start it re-joins and re-normalizes the growing window from scratch (`_normalize(_strip_latex(" ".join(cum_text)))` in the innermost loop, extract.py:610–611), and every scored window is appended to a global list (extract.py:584, 615) fully sorted at the end (extract.py:630) although only the global best (extract.py:631) and best-per-file top-3 (extract.py:645–656) are consumed. Separately, `assert_source_pdf_unchanged` MD5s the full source PDF (state.py:656, chunked read state.py:620–625) and all eight apply.py mutators call it via `_guard_source_pdf` (apply.py:248, 446, 580, 636, 695, 726, 762, 798) — including `set_current_annotation`, the status-neutral cursor move behind the viewer's Prev/Next (apply.py:680–686). `apply_batch` loops `apply_edit` per edit (apply.py:395–397): N edits ⇒ N full-PDF hashes plus 2N atomic writes (apply.py:341–342 per call).
- JUDGEMENT: Cost is O(annotations × files × lines × window-length) for extract and a full-file hash per Prev/Next click. Strategically, an "index the corpus once, resolve many snippets" decomposition is exactly the shape a unified cross-format anchor resolver needs (the desktop's `md-fuzzy-snippet` anchors solve the same problem), so the refactor pays twice.
- RECOMMENDATION: Split into `build_window_index(root)` + `resolve(target, index)`, tracking best-overall/best-per-file during the scan (M). Cache the PDF guard per (path, mtime, size) and have `apply_batch` guard once then call an unguarded core (S).

**Reusability for the multi-format direction.** FACT: `apply_edit`/`revert_edit` treat the target purely as a list of text lines — nothing LaTeX-specific beyond the field name `latex_file` (apply.py:261, 279–300); `fuzzy_map`'s LaTeX coupling is exactly two points — the `_strip_latex` regex pass (extract.py:514–516, applied at :611) and the `"*.tex"` glob (extract.py:530); the status-transition table references no format at all (state.py:492–598). But `read_annotations` captures annotation subtype only into a function-local (extract.py:308, 354), used solely for sticky-note association (extract.py:370); the persisted `Annotation` has no subtype field (state.py:191–199), and nothing read at extract.py:317–364 captures reply chains (IRT) or native PDF annotation ids. JUDGEMENT: the transition table, atomic-write/StateDir layer, line-edit primitives, and a generalized fuzzy resolver all carry over — but as persisted, extraction cannot even distinguish a Highlight from a StrikeOut, so annotations.json schema-v2 is unavoidable for the Acrobat round-trip. RECOMMENDATION: when the schema bumps, persist `subtype`, native annotation id, and optional `in_reply_to`; rename `latex_file` → `file`; parameterize `fuzzy_map`'s glob + strip function per format — ride the same schema-v2 change as the unified model. M.

**Also noted:** the status enum and terminal/non-terminal partition are defined four times across three modules (state.py:173–183, 433–438; status.py:31–41, 103–110 — which silently ignores unknown statuses per status.py:97–101; commit.py:91–101) — export canonical sets from state.py (S); duplicated chunked-MD5 helpers (extract.py:817–823 vs state.py:620–625) and dangling "chunk A/C"/"finding-8" docstring references that point at a build plan, not the code (apply.py:82–84, :331; state.py:480–484, :512) (S); the extract `--json` summary labels `sum(1 for a in annotations if a.trigger_match)` as "surfaced_pending" (extract.py:954–956, :965) though bootstrap assigns that status only when `not ann.trigger_match` (extract.py:714–716) — disjoint populations (S); state.json/mapping.json pair writes and commit-then-advance ordering are not cross-file transactional (apply.py:341–342, :529–530; commit.py:329–352 — a post-commit failure makes re-run produce a second commit) — document the crash-recovery story and make commit-phase re-run idempotent (S–M). The schema-version-guard bypass by these same mutators is consolidated under "Engine — CLI, Tests & Cross-Cutting" below.

## Engine — Server, Build & Terminal

### Viewer auto-reload contract (HEAD /api/state) is unimplemented — the legacy viewer's core loop is broken [high / S]

- FACT: templates/frame.html:666-669 documents the reload mechanism: "Polls HEAD /api/state every 500ms (spec §10.2); on Last-Modified change, reloads. Server contract (chunk D): HEAD /api/state must set Last-Modified to the state.json mtime, or ETag to its md5." The poll at frame.html:672-679 computes `tag = r.headers.get("etag") || r.headers.get("last-modified") || ""` and reloads only when the tag changes. But server.py implements only `do_GET` (server.py:265) and `do_POST` (server.py:288); `grep -rn 'do_HEAD' src/ tests/` returns no hits, and `grep -rn 'Last-Modified\|ETag\|etag' src/review_pdf_to_latex/server.py tests/test_server.py` returns nothing. `_send_bytes` (server.py:674-680) sends only Content-Type/Content-Length/Cache-Control. Because `ReviewHandler` subclasses `http.server.SimpleHTTPRequestHandler` (server.py:255), HEAD requests fall through to the stdlib filesystem handler rooted at the server cwd. ✓ (C4, Pass-6 verified: frame.html:675 falls back to `tag=""`, so the :676 reload condition can never trigger)
- JUDGEMENT: The tag is permanently `""`, so the page never auto-reloads — the click→engine→reload loop the in-page comments describe (frame.html:462-465) cannot fire; buttons disable on click (frame.html:540-543) and only the watchdog re-enables them. test_server.py (1,554 LOC, 68 tests) never issues a HEAD request, so the rot is invisible to the suite. Secondary: the fallthrough means `HEAD /<anything>` probes arbitrary files under the server cwd (existence/size/mtime disclosure; localhost-only, but unintended).
- RECOMMENDATION: Decide keep-vs-kill first (next finding). If kept: implement `do_HEAD` with a stat-based Last-Modified/ETag responder and switch to `BaseHTTPRequestHandler` to close the fallthrough (S). If killed: this is the headstone evidence.

### Keep-vs-kill: split the format-agnostic event bus out of server.py, then freeze or retire the viewer [high / M]

- FACT: server.py is two unrelated programs in one 1222-LOC module: (1) the HTML viewer (`ReviewHandler` + `_render_frame` + page/build/static serving, server.py:255-690) and (2) a format-agnostic event pipeline — `_validate_event` (server.py:96-169), `_append_event_line` with flock (server.py:234-252), `wait_for_events` + watchers (server.py:838-1004), `handle_wait_event` (server.py:1179-1223) — the consumer API the CLI wires (cli.py:325-327). The viewer half is superseded by the Electron app (ground-truth map: two parallel viewer stacks) and demonstrably rotted (reload finding above; diff2html in the tail below). Reusable inventory verified: `pdf_health.py` is a pure `(pdf_path) -> (exit_code, report)` function with its own `schema_version` (pdf_health.py:65, 166-167) and the desktop ALREADY shells into it (desktop/main/engine.ts:265: `runEngine(['pdf-health', '--pdf', pdfPath, '--json'] …)`) — the best adapter precedent for the unified model. `compute_page_md5s`/`paginate_diff` (build.py:164-238) are PDF-generic and LaTeX-free. terminal.py (456 LOC of hand-rolled RFC 6455 + pty, terminal.py:9-14) exists only to serve the legacy viewer's embedded pane and duplicates desktop/main/claude-pty.ts across layers (Pass 3B). One contract bug rides along: `wait-event --since` filters strictly `ts > since_ts` on second-resolution timestamps (server.py:87-93 `timespec="seconds"`; server.py:932) while the documented compaction-recovery pattern re-calls with `--since <last_observed_ts>` (server.py:1020-1022) — two same-second events straddling a consumer restart silently drop the second one; also `wait_for_events` claims "Side effects: NONE" (server.py:851) but the kqueue watcher touches the events file when absent (server.py:977-980).
- JUDGEMENT: The capability direction needs the engine's flock-disciplined JSONL event bus (the closest existing thing to a cross-layer comment-event bus) and pdf_health's clean adapter shape — not the HTML viewer or its WebSocket terminal. The survivable ~40% is fused to a rotting ~60%, and terminal.py + 831-line frame.html + 283 KB vendored xterm are carrying cost for a UI the owner no longer drives.
- RECOMMENDATION: Extract `_validate_event`/`_append_event_line`/`wait_for_events`/`handle_wait_event` into an `events.py` with zero viewer imports, fixing the `--since` cursor (sub-second timestamps or a sequence number) in the move (M). Then make the explicit Phase-B call between (a) deleting server-viewer + terminal.py + templates (~2.6K LOC + vendor) keeping only `wait-event`, or (b) freezing them behind a "legacy, headless-only" note with the reload bug fixed. Evidence favors (a) unless a headless use-case is documented.

### preview() returns a guessed build ID and ignores build failure — read-then-rebuild TOCTOU [med / S]

- FACT: `_invoke_build` (preview.py:186-195) reads state.json and computes `build_id = build_mod.next_build_id(state)` BEFORE calling `run_build_command`, which independently re-reads state and recomputes its own ID (build.py:337-338); nothing flows the actual ID back — `run_build_command` returns only an int exit code (build.py:298). `next_build_id` is `len(state["builds"]) + 1` (build.py:129-138). The window spans a full LaTeX compile (2 passes × 120 s: build.py:79 `_LATEX_PASSES = 2`, build.py:349 `timeout_sec=120`).
- JUDGEMENT: Any builds[] append between the two reads makes `preview()` return an ID pointing at the wrong artifact — the viewer displays the wrong speculative build. And since the exit code is ignored, a failed compile still returns a successful-looking build ID.
- RECOMMENDATION: Have `run_build_command` return (exit_code, build_id) — or add a `run_build(...) -> BuildResult` API — and have preview() use the real ID and propagate failure. S.

### Speculative in-place edit can destroy concurrent edits and leaves no on-disk snapshot across a crash [med / M]

- FACT: `with_in_place_edit` snapshots the .tex file only in memory (preview.py:121 `original_bytes = latex_file.read_bytes()`), writes the speculative text to the real file (preview.py:126), runs the build, then unconditionally restores in `finally` (preview.py:131-132). The recovery dump is written only when the restore raises OSError (preview.py:133-140). The mutation window spans the compile (build.py:79, 349).
- JUDGEMENT: (a) Any external edit during the window — the owner's editor, or a future desktop editing surface — is silently overwritten by the restore. (b) On SIGKILL/power-loss mid-build, `finally` never runs and the file is left containing speculative text with the only snapshot lost with the process; the dual-failure message (preview.py:146-147) admits "from git or backup", but the crash case has no message at all.
- RECOMMENDATION: Write the snapshot to `.review-state/preview-recovery-…` BEFORE mutating (delete on clean restore), and compare file bytes to the speculative text before restoring, refusing to clobber third-party changes. M.

**Also noted:** `_render_frame` (server.py:415-534, ~120 LOC) mixes unresolved-status filtering, default-annotation selection, and saved-id fallback with Jinja context assembly, and `_resolve_navigate_target` re-implements the identical visibility rule as its own closure (server.py:450-467 vs server.py:172-231, :200-220) — extract one `resolve_visible()` used by both; this locates the ground-truth map's `_serve_frame` suspicion precisely (the mixing is in `_render_frame`; `_serve_frame` is 10 lines, server.py:404-413) (S). `_atomic_write_state` (server.py:1060-1082) duplicates `state.atomic_write_json` line-for-line, and server.py:37's comment "mirrors state.STATE_DIR_NAME for module independence" references a constant that does not exist (canonical is `StateDir.DIR_NAME`, state.py:29) while server.py:34 already imports from state — the literal `".review-state"` is also hard-coded at build.py:319 and preview.py:51,54 (S). state.json read-modify-write has no inter-process lock while the events file is flock-serialized (server.py:239-241 vs build.py:337/391-392, server.py:1111-1114) — lost-update risk that becomes real once desktop and engine co-write the file (M). diff2html was never vendored, so the redraft diff pane is silently dead in every install (server.py:495 gate; `ls templates/static/` shows only .gitkeep + xterm assets; annotation.html:59) (S). Mid-file/function-local module imports at server.py:1027-1029, preview.py:155, build.py:314-316 (S).

## Engine — CLI, Tests & Cross-Cutting

**Coverage map.** FACT: `wc -l tests/*.py` → 22 files, 8,648 LOC; `grep -c 'def test_' tests/*.py` → 354 test functions. Every engine module has a dedicated test file except `__main__.py` (a trivial 16-LOC shim, `__main__.py:12-16` — acceptable gap). Largest: test_server.py 1554 LOC/68 tests, test_cli.py 1163/42, test_extract.py 1162/43. Meta/contract tests pin the README (tests/test_readme.py:18), CHANGELOG, and the polecat SKILL.md (tests/test_skill.py:1-6); the e2e suite is opt-out (`pytest.mark.slow` + pdflatex skipif, tests/test_e2e.py:39-42). JUDGEMENT: unusually strong coverage for a solo project; the holes are specific and listed below.

### Schema-version guard is bypassed by every mutator and uncaught when it fires — migration design can't work [high / M]

- FACT: `state.read_json` enforces the schema contract (SchemaVersionError at state.py:149-151, MigrationRequiredError at state.py:159-160). But apply.py defines its own unguarded reader (apply.py:121-123, bare `json.load`) used for every mutation-path read (apply.py:148, 584, 646-647, 699, 730, 766, 803); commit.py uses bare `json.load` at commit.py:227 and 282; server.py uses bare `json.loads(...read_text())` at server.py:194-195, 429-431, 1111; `assert_source_pdf_unchanged` raw-loads annotations.json (state.py:643-644). Only build.py:337, preview.py:186/229, and status.py:92 go through the guarded reader — i.e. only read-only/secondary paths. `grep -n 'SchemaVersionError\|MigrationRequiredError' src/review_pdf_to_latex/cli.py tests/test_cli.py` returns NOTHING — no CLI handler catches either exception, and exit 14 exists only for the migrate-state subcommand itself (cli.py:810-832). migrate.py is an empty registry stub whose design depends on the read-time guard tripping (migrate.py:13-16: a partial migration "still trips the 'needs migration' check on next read"). ✓ (C3, Pass-6 verified)
- JUDGEMENT: The modules that WRITE state are exactly the ones that never check `schema_version` — a schema-bumped or version-less state.json is silently mutated and rewritten by apply/set-status/commit-phase, and where the guard does fire (status/build/preview) it escapes as a raw traceback with generic exit 1. The entire migrate-state safety story protects only the harmless paths — exactly backwards for the schema-v2 change the unified comment model will force.
- RECOMMENDATION: (1) Delete `apply._read_json` and route apply/commit/server state reads through `state.read_json`, wrapping the schema errors into ApplyError/CommitError subclasses per the existing `_guard_source_pdf` wrapping pattern (apply.py:79-93); (2) add a top-level catch in cli.main mapping SchemaVersionError/MigrationRequiredError to a spec'd exit code. M overall; the reader swap alone is S.

### Exit-code and error-mapping contract expressed four different ways, with a live numeric overload and two semantically wrong mappings [med / S]

- FACT: The spec-§8 contract appears as (a) named constants in cli.py:810-832, pinned by tests/test_cli.py:1134 `test_exit_code_constants_match_spec`; (b) hardcoded class attributes — apply.py:33-77 (`exit_code = 7…22`), commit.py:24-50 (`exit_code = 15…22`); (c) bare magic numbers — extract.py:869/885/904/911, server.py:1107/1213; (d) pdf_health.py:61-63 where `EXIT_ENCRYPTED = 21` — the same number cli.py:830 assigns to `EXIT_SOURCE_PDF_CHANGED` — returned as the process exit code (pdf_health.py:259-262). No test cross-checks (b)/(c)/(d) against (a): `grep -rn '\.exit_code' tests/test_apply.py tests/test_commit.py` → empty. Two error-mapping conventions coexist: exceptions carrying their exit code with handlers doing `return exc.exit_code` (cli.py:469-471 and seven more sites) vs preview's per-class manual mapping (cli.py:668-688), forced because preview.py:158-170 defines its own plain-Exception twins outside the ApplyError hierarchy — including a duplicate `AnnotationNotFoundError` (preview.py:158 vs apply.py:38). Two handlers also remap ValueError to `EXIT_ILLEGAL_STATUS_TRANSITION` (18) for non-transition errors (cli.py:500-503 revert; cli.py:561-564 append-chat). The desktop consumes these codes as a bare numeric set (desktop/main/engine.ts:273 — Pass 3A coupling).
- JUDGEMENT: Exit 21 already means two different things depending on subcommand; renumbering pressure or a copy-paste slip in the three unpinned notations breaks the engine↔skill↔desktop contract invisibly; every new subcommand author must pick a convention.
- RECOMMENDATION: Extract a single `exit_codes.py` imported by cli/apply/commit/extract/server/pdf_health; fold preview's exceptions into the exit-code-carrying hierarchy so all handlers collapse to `return exc.exit_code`; extend the pinning test to assert exception-class codes match; resolve or document the 21 overload; give the two ValueError cases honest codes. S.

### bulk-surface and wait-event are the only untested subcommands on an otherwise pinned 17-command surface [med / S]

- FACT: `grep -rn 'bulk' tests/ src/review_pdf_to_latex/apply.py` returns only apply.py:604 (`def bulk_surface_pending`) and apply.py:752 (a docstring) — no test mentions bulk-surface. For wait-event, `grep -rn 'handle_wait_event' tests/` hits only a comment block; test_server.py:1226-1227 admits the deferred end-to-end suite was never added (only signal-handling primitives are tested, test_server.py:1249). Both are contract subcommands consumed by the skill (cli.py:262-269; cli.py:254-259).
- RECOMMENDATION: Add a small test module each (happy path + documented exit codes: bulk-surface 0/18/21/22 per cli.py:697-698; wait-event 0/20); the conftest project fixtures already exist. S.

### `--json` is a global flag that most subcommands silently ignore; errors are never JSON [low–med / S]

- FACT: `--json` is attached to the top-level parser and every subparser (cli.py:76-82, help text "where supported"). Only three handlers read it: extract (cli.py:304), status (cli.py:443), bulk-surface (cli.py:709); pdf-health is always-JSON by design (cli.py:285-286). apply/revert/set-status/set-current/append-chat/record-proposal/override-mapping/commit-phase/preview never reference `json_output` (verified across cli.py:450-690). All error paths print free text to stderr (e.g. cli.py:470).
- JUDGEMENT: The Electron app and the skill drive this CLI programmatically; today they get machine output from 4 of 17 subcommands and must parse prose stderr for failures. The unified-model direction adds more programmatic callers.
- RECOMMENDATION: Honor `--json` uniformly (success envelope + structured `{error, exit_code}` on stderr) or scope the flag to supporting subcommands so `apply --json` errors loudly. Minimal S step: the structured error envelope.

**Positives worth keeping.** FACT: every handler lazy-imports its module inside the function (cli.py:296, 312, 325, 337, 454, 637) — `review-pdf status` doesn't pay for pdfplumber/jinja2 import; good per-operation cost discipline for a subprocess-per-call consumer. The dispatch table with a documented 3-step extension recipe (cli.py:3-6, 754-756) and the spec-pinning exit-code test (test_cli.py:1134-1141) are a solid contract backbone. `--project-dir`/`--json` accepted before or after the subcommand via the argparse.SUPPRESS trick (cli.py:49-66), and it's tested (test_cli.py:73, 105, 118). Dependency hygiene is clean: runtime deps are exactly pdfannots/pdfplumber/rapidfuzz/jinja2 (pyproject.toml:28-33) with no undeclared imports (import sweep); uv.lock is real and current (uv.lock:732-745; lock commit 2390925 postdates last pyproject change 6e616f6, verified via `git log -1 -- <file>`).

**Reusability (JUDGEMENT, grounded above).** The CLI router is already format-agnostic at the dispatch layer: only `extract --pdf` (cli.py:109) and build's LaTeX engines (cli.py:166-169) are format-coupled; every state-mutation subcommand (set-status, append-chat, record-proposal, override-mapping, set-current) operates on annotation IDs and carries over to a unified comment model unchanged. The exit-code contract + `--json` envelope is the natural engine↔desktop seam — which is why the schema-guard, exit-code-single-source, and uniform-JSON findings are the cheapest pre-work for that direction. `apply_batch` (apply.py:353) is a ready-made bulk-apply primitive if the unified model needs one.

**Also noted:** cli.py's docstring advertises "14 subcommands" while `_HANDLERS_TABLE` has 17 (cli.py:1-3 vs cli.py:757-775; the ground-truth map's "16" is also off by one) (S); `apply_batch` has no production caller (`grep -n 'apply_batch' src/ tests/` → apply.py:353 + tests/test_apply.py:264/317 only; cli routes apply to `apply_edit`, cli.py:454) and `_PHASE_SHORT_TO_FULL` is 6/11 keys unreachable (cli.py:621-632, lookup :640, argparse choices restricted to `["1","2a","2b","3"]` at cli.py:244) — wire it to a real `apply --batch` or delete, keeping it only if the unified model wants bulk apply (S); pytest-cov is declared (pyproject.toml:38) but no `--cov` is wired anywhere (pyproject.toml:52; no Makefile/.github exist) — the suite has no measured coverage, which would have surfaced the bulk-surface/wait-event gaps instantly (S); pinning is loose `>=` throughout (pyproject.toml:29-38; lockfile mitigates dev installs only — known from the hygiene block); `_reviewer_rig_guard` (cli.py:24-46, exit 23) covers apply/build/revert (cli.py:310, 452, 484) but not commit-phase (cli.py:635-652), which runs `git commit` on the source repo — possibly deliberate per spec §10.5.2, confirm and either guard or document the exemption (S); `_HANDLERS_TABLE: dict[str, "callable"]` annotates the builtin, not typing.Callable (cli.py:757 — cosmetic).

## Desktop Main — Process, IPC & Engine Bridge

**Pass 3 — key takeaways** (covers all three Desktop Main sections)

- The "typed IPC" surface is vacuous on the result side and has already drifted on both bridges: `EngineResult`/`PdfHealthReport` exist as two unlinked copies behind a false "re-exported" comment, and the renderer's stale hand-redeclared `AgentViewerApi` forces `as any` casts on every newer agent feature (engine.ts:21-68/237-253 vs shared/types.ts:4-80; ipc-client.ts:7-26).
- The comment data model is the single biggest seam blocking the unified-model centerpiece: `CommentPayload.anchor` is a REQUIRED PDF-shaped field on every comment in every format, md is an optional bolt-on, the format discriminator sits at FILE level — and bundle.ts already consumes it blind, so only renderer call-site discipline keeps a non-PDF comment out of the PDF annotation writer (types.ts:121-130, 157-170, 190; bundle.ts:67-68, 203-204).
- The two live Claude routes are a behavioral fork, not just duplicate code — they diverge on permissions, cwd, worker caps, and resume — and an errored SDK session becomes a zombie that silently eats every subsequent message (claude-pty.ts:219-220 vs claude-backend.ts:160-161; agent-pane-ipc.ts:41-43, claude-backend.ts:173-185).
- Three S-sized correctness bugs sit in the round/quit lifecycle: the abandoned-round tombstone still matches the results-watcher regex and resurrects the round; `gt mail`'s stdin has no error listener so an early gt exit EPIPEs into an uncaught main-process exception; two independent `before-quit` handlers tear subsystems down during the *prevented* quit and re-run unawaited on the real one (results-watcher.ts:44 vs submit.ts:323; submit.ts:253-262; main/index.ts:282-292 + 818-822).
- Test investment is inverted: zero coverage on engine.ts, main/index.ts, preload/, submit.ts, bundle.ts, and results-watcher.ts — the modules that mutate user files and drive the round lifecycle — while the one module scheduled for deletion (sidecar-migration) is the best-covered; pure logic cores throughout make this cheap to fix.

---

### The hand-wired typed IPC surface is vacuous on the result side and has already drifted on both bridges [high / S–M]

- FACT: `EngineResult`, `ResolutionAttempt`/`ResolutionStep`, `PdfHealthReport`, and `PdfHealthResult` are each defined twice, verbatim-parallel: desktop/main/engine.ts:21-68 + 237-253 vs desktop/shared/types.ts:4-80. engine.ts:234-235 claims "Re-exported from shared/types so callers in main can import from one place" — but the lines below it (engine.ts:237-249) are a local re-definition, and engine.ts imports nothing from @shared (imports are node builtins only, engine.ts:15-19). Nothing links the two copies: handlers return engine.ts's types through untyped `ipcMain.handle` callbacks (main/index.ts:360, :366), and on the renderer side `ipcRenderer.invoke(...)` returns `Promise<any>`, so the `const electronAPI: ElectronAPI` annotation (preload/index.ts:24) type-checks parameters but vacuously accepts ANY result shape for all ~30 invoke wrappers (e.g. preload/index.ts:25-27). The second bridge has already drifted: the preload defines the real 12-method `agentViewer` API (incl. `notifyDocSwitch`, `freshStart`, `spawnSession`, `listSessions`, all with optional `sessionId`, preload/index.ts:138-205) and exports its type (preload/index.ts:209), but no renderer file imports it — renderer/agent-pane/ipc-client.ts:7-20 hand-redeclares a stale 8-method `AgentViewerApi` (no sessionId params, none of the four newer methods) and binds it to `window.agentViewer` (ipc-client.ts:22-26), so newer call sites cast around their own global type: `(window as any).agentViewer?.spawnSession(...)` (renderer/toolbar.ts:344, :393), an inline structural cast for `freshStart` (toolbar.ts:433-442), another for `notifyDocSwitch` (renderer/index.ts:1346, :1353).
- JUDGEMENT: A change to any handler's return shape in main compiles cleanly while the renderer keeps consuming the stale shared/types.ts copy — exactly the drift class strict-mode TS was adopted to prevent — and the agentViewer bridge proves the strategy drifts in practice: every post-M-int-3 agent feature crossed the bridge untyped. The duplication is unforced (main/index.ts already imports from '@shared/types', main/index.ts:18-41).
- RECOMMENDATION: Delete the engine.ts copies and import from @shared/types (S, mechanical). Move the agentViewer bridge type to shared/ (alongside shared/agent-pane/types.ts), have BOTH preload/index.ts and ipc-client.ts import it, delete the three casts (S). Then add enforcement once for the whole surface: a `handle<K extends keyof ElectronAPI>(channel, fn)` wrapper (or a channel→{req,res} type map) so `ipcMain.handle` bodies are checked against the same ElectronAPI contract the preload implements (M). Land this before the multi-format work adds docx/html comment channels.

### shared/types.ts is a 1008-line five-concern type god-file; the comment model inside it accretes per-format fields instead of a discriminated anchor union [med / M]

- FACT: One file mixes (1) engine subprocess types (types.ts:4-80, the duplicates above), (2) the comment/drafts domain schema (CommentPayload 126-166, AnchorKind 170, DraftsFile 186-192), (3) on-disk file schemas for submit/results/bundle/appState (211-268, 349-385, 405-470), (4) the 150-line `ElectronAPI` interface (618-767) plus per-channel result unions, and (5) the pty/worker subsystem (769-1008, ~240 LOC) including runtime constants (`REVIEWER_LOCAL_ID` 483, `MAX_WORKER_PTYS` 942, `MAX_WORKER_TABS` 944) and the `declare global` Window binding (1004-1008). The agent-pane subsystem already does it the other way — its types live in shared/agent-pane/. Within the comment model: `CommentPayload.anchor` is a REQUIRED PDF-shaped `AnchorRegion` (`page` + rect, types.ts:121-124, 130) for every comment regardless of format, the md anchor is a bolted-on optional `md_anchor?` blob (157-165), and the format discriminator `anchor_kind` lives at FILE level on DraftsFile (190), not per comment. The file header's own rule ("Keep this surface minimal — add types here only when both sides genuinely need them", types.ts:1-2) is long broken.
- JUDGEMENT: The unified cross-format comment model is the charter's centerpiece, and this file is where it will live. Today adding DOCX/HTML comments means a third optional `*_anchor?` field and more file-level flags — accretion, not polymorphism. Lane 3C found the live consumer of this flaw: the bundle writer reads the PDF anchor with no discriminator check (see "Persistence & Submit Pipeline" below).
- RECOMMENDATION: Split along the five seams (shared/engine.ts, shared/comments.ts, shared/files.ts, shared/ipc.ts, shared/pty.ts — mirroring the shared/agent-pane/ precedent). In shared/comments.ts, restructure the anchor as a per-comment discriminated union `anchor: { kind: 'pdf-glyph-rect', … } | { kind: 'md-fuzzy-snippet', … } | …` with a sidecar-migration shim for existing files. M; do it as the FIRST step of the unified-model work, not after.

### main/index.ts registers the IPC surface in three styles, and its two independent before-quit handlers double-run teardown [med / M]

- FACT: One 826-LOC entry file registers ~25 channels inside a single `app.whenReady().then(async () => {...})` closure (main/index.ts:339-813) in three styles: (a) full inline bodies for fs/dialog/drafts/appState — e.g. fs:readPdfBytes (387-414), fs:listDir (524-565), fs:indexPdfs (704-747), fs:writeFileText (588-613); (b) one-line delegations to modules for bundle/submit/results (777-799); (c) self-registering subsystem registrars `registerClaudePtyIpc()` (802) vs `registerAgentPaneIpc(mainWin)` (808). Besides handlers, the file inlines the external-open queue + reviewpdf:// URL + argv parsing (92-185), the drafts flush handshake (235-292), and single-instance/protocol setup (300-337). It also registers two `before-quit` listeners: the first (282-292) sets a flag, calls `event.preventDefault()`, awaits the drafts flush handshake (2 s cap, FLUSH_TIMEOUT_MS :235), then calls `app.quit()` again; the second (818-822) runs `stopResultsWatch(); shutdownClaudePty(); await shutdownAgentPane();`. Electron runs both listeners on the SAME emission, so on the first (prevented) quit the pty/agent subsystems are already torn down while the app keeps running for up to 2 s; on the re-issued quit the second handler runs AGAIN, and its `await` is on an event listener Electron does not await, so `shutdownAgentPane()` races process exit.
- JUDGEMENT: Style (b)/(c) is already the house pattern; the inline cluster (~280 LOC of pure fs/path logic plus ~200 LOC of launch plumbing) is what makes the file a god-file and leaves the fs primitives untestable in isolation. Quit correctness today is coincidental — teardown happens to be survivable and the prevented-quit window happens to give the async shutdown time; any reordering breaks silently.
- RECOMMENDATION: Extract `main/fs-ipc.ts` (readPdfBytes/listDir/pathExists/writeFileText/watchFile/indexPdfs + classifyFile/isHiddenName), `main/external-open.ts` (92-185 + 300-337), and `main/quit-flush.ts` (235-292), each a registrar in style (c); index.ts becomes window creation + registrar calls. Collapse to ONE before-quit handler that preventDefaults, then sequentially `await flushAll(); await shutdownAgentPane(); shutdownClaudePty(); stopResultsWatch();` and only then re-quits via a `quitting` flag. M (quit collapse alone S).

### No runtime validation on any of ~55 channels, and the generic fs read/write channels make the sandbox boundary advisory [med / M]

- FACT: The preload declares itself the security boundary ("Anything the renderer can call must be declared here — this is the security boundary", preload/index.ts:22-23) and window hardening is genuinely strong (`contextIsolation: true, nodeIntegration: false, sandbox: true`, main/index.ts:198-203). But no handler validates its arguments at runtime (`fs:writeFileText` writes caller-supplied `content` to any caller-supplied absolute path, main/index.ts:588-613; `fs:readPdfBytes` returns the bytes of ANY path, 387-414), and `grep -rn 'will-navigate\|setWindowOpenHandler\|will-attach-webview' main/ preload/` returns nothing — no navigation/window-open guard exists. Channel-name drift is currently ZERO: a full channel-set diff between preload and main (grep over both, then `comm`) showed no orphans either way — but nothing enforces that beyond re-running the grep.
- JUDGEMENT: With sandboxed renderers displaying untrusted local HTML/DOCX-derived content (html-viewer/docx-viewer iframes), a renderer compromise escalates directly to arbitrary file read/write via these channels; the missing nav guard is the cheap half of Electron's standard checklist. For a local single-user tool this is hardening, not an incident — hence med, not high.
- RECOMMENDATION: Add `will-navigate` deny + `setWindowOpenHandler(() => ({action: 'deny'}))` in createWindow (S). For validation, don't hand-write zod everywhere: scope fs:writeFileText/readPdfBytes to the opened root + `.review-state/` paths, and fold per-channel runtime checks into the same typed `handle()` wrapper recommended in the first finding (M).

### engine.ts bridge: per-call re-resolution anchored to a nondeterministic cwd, a hand-mirrored exit-code/schema contract, and zero tests [med / S–M]

- FACT: `runEngine` calls `resolveEngine()` unconditionally per invocation (engine.ts:152-153); the chain probes every PATH dir with `access()` (engine.ts:82-90) plus up to 4 cwd-ascending `.venv` candidates (121-128) and the home venv (130-134) — no caching anywhere in the file. Step 3 resolves relative to `process.cwd()` with the in-code admission "In a packaged app, cwd is wherever the user launched from" (engine.ts:119-120). Both IPC consumers re-trigger it: `engine:version` and `engine:pdfHealth` (main/index.ts:360, 366), the latter invoked at every PDF load (renderer/index.ts:1286 via grep). The exit-code contract is a bare numeric set — `const successfulExits = new Set([0, 2, 21]);` (engine.ts:271, membership test :273) — including the `21` Pass 2 showed is overloaded engine-side (EXIT_ENCRYPTED in pdf_health.py:61-63 vs EXIT_SOURCE_PDF_CHANGED in cli.py:830). `PdfHealthReport` (engine.ts:237-249 / types.ts:58-70) hand-mirrors the Python dict with a comment-only link (types.ts:55), and the parse is an unvalidated `JSON.parse(stdout) as PdfHealthReport` (engine.ts:279); the same function carries a no-op ternary `const stdout = engine.ok ? engine.stdout : engine.stdout;` (engine.ts:275). Coverage: `ls main/*.test.ts shared/**/*.test.ts` → only agent-pane-ipc, sidecar-migration, adapter, anchors tests — nothing for engine.ts, main/index.ts, or preload/, though engine.ts is dependency-light pure logic with an injectable env hook already present (`RunOptions.env`, engine.ts:140-145).
- JUDGEMENT: This is the desktop end of the engine↔desktop contract seam Pass 2 flagged: three hand-synced copies (Python constants, TS numeric set, TS interface) with zero tests on the TS side — the first engine renumbering or report-field change fails silently at runtime. cwd-relative resolution means the packaged app finds different engines depending on launch origin, and per-call re-resolution is the cost the multi-format direction will multiply across every engine-backed comment operation.
- RECOMMENDATION: Memoize resolution per app run (invalidate on REVIEW_PDF_ENGINE_PATH change) and resolve the repo-venv step against the doc/project dir handed in by the caller (S). When the engine grows its single `exit_codes.py` (Pass 2 rec), emit/check a TS constants twin and pin both with a contract test; validate the parsed report minimally (`schema_version === 1`) before the cast (S). Add engine.test.ts (resolution order with temp dirs, timeout/spawn-fail mapping, pdfHealth exit-code matrix) plus a registration smoke test asserting the main-side channel set equals the preload set (S–M).

**Also noted (cited; one S-sized sweep for the first cluster):** API residue says "PDF tool" while behavior is already multi-format — `drafts:read`/`drafts:write` still take a dead `_sha256` param (main/index.ts:420, :479) that preload requires from every caller (preload/index.ts:30-33) while doc comments still describe the pre-migration sha256 layout (types.ts:102-103, :635); the argv positional/drag form matches ONLY `.endsWith('.pdf')` (main/index.ts:182) though `classifyFile` knows md/html/docx (68-75); `fs:indexPdfs` filters to pdf only (736) so Cmd+P indexes one of four viewable formats; and `fs:readPdfBytes` is the app's generic byte loader (five renderer call sites: renderer/index.ts:210, 1285, 1415, 1545, 1632 via grep) under a pdf-specific name and result type (types.ts:91-93) — drop the `_sha256` params, fix the comments, rename to `fs:readFileBytes`, extend argv/index filtering to the FileKind set. Smaller: `fs:watchFile` returns bare `void` with a swallowed catch (main/index.ts:620-633), keeps a write-only `fileWatchPath` (set :623, :637; never read), and uses a 1 s wall-clock suppression window (:642-644) racing slow writes; dialog handlers cast `win ?? undefined as any` twice (:373, :512); the `ping` smoke channel and `'review-pdf — empty shell'` title survive from M1 (:196, 344-346; masked by renderer/index.html:15); `npm run typecheck`'s first invocation targets the solution-style tsconfig with `"files": []` — a no-op without `--build`; engine timeout kills with SIGTERM only, no SIGKILL escalation (engine.ts:191-194) unlike the pty layer's documented 1.5 s fallback (types.ts:737-738). Cross-lane: PATH-binary resolution now exists 3× (engine.ts:82-90; claude-pty.ts:89-105 `whichSync`; submit.ts:51-69 `whichGt`, a copy with `gt` hardcoded per its own comment submit.ts:47-50) — consolidate when fs utils are extracted.

**Reusability for the multi-format direction (JUDGEMENT, grounded above).** The backbone is in better shape than its naming: `runEngine`/`EngineResult` (engine.ts:152-222) is already the generic engine adapter the unified model needs, with `pdfHealth` (264-288) as the proven wrap-a-subcommand template — fix the duplication/exit-code/caching findings and it generalizes to `applyComment`/`extractAnnotations` calls unchanged. The Result-union error convention (~20 handlers) and the atomic tmp+rename write pattern are format-agnostic house style worth keeping (the three inline copies in main/index.ts are consolidated under the Persistence section's duplication finding). The preload's dispose-function event-subscription pattern (preload/index.ts:34-38 et al.) scales cleanly to per-format comment events. What does NOT carry over as-is: the PDF-shaped required `anchor` and file-level `anchor_kind` (types.ts:126-130, 190) — the per-comment discriminated-union restructure is the single highest-leverage type change before DOCX/HTML comment channels are added.

## Desktop Main — Claude/Agent Subsystem

Context (per domain trap #6): the legacy xterm pty route and the React/SDK agent route are BOTH live behind `localStorage.getItem('pdf-latex-new-agent-pane')` (renderer/toolbar.ts:35, renderer/index.ts:40); neither is dead code. These findings assess the *cost of divergence* between the two live routes, not their existence.

### Two live Claude routes have materially divergent semantics, not just duplicate code [high / L]

- FACT: The same four features are implemented twice with different behavior. (1) **Permissions**: the pty route defaults to `--dangerously-skip-permissions` (claude-pty.ts:219-220 `const skipPerms = params.dangerouslySkipPermissions !== false;`, again at :526-527); the SDK route hardcodes `permissionMode: "default"` with an interactive `canUseTool` approve/deny flow (claude-backend.ts:160-161, re-verified this session); the agent IPC payloads (`agent:send`/`agent:freshStart`/`agent:spawnSession`, agent-pane-ipc.ts:116-128, 216-228, 234-255) carry no skip-permissions field, so the Settings toggle (commit 8a98733) only affects the pty route. (2) **Working directory**: pty route anchors cwd to the doc source dir per §9.2.9 (claude-pty.ts:209-210, 516-517); the SDK route never sets cwd — `query()` options (claude-backend.ts:157-166) contain only `permissionMode`, `canUseTool`, `includePartialMessages`, `resume`, `model` — SDK sessions run in the Electron process cwd. ✓ (C11, Pass-6 verified: zero `cwd` in claude-backend.ts; the only other `query()` at :276 is the AGENT_VIEWER_SMOKE debug one-shot, not the session route) (3) **Worker caps & sling gating**: pty route enforces `MAX_WORKER_PTYS` = 16 (claude-pty.ts:494-500; shared/types.ts:942) and refuses sling without gt + destination (claude-pty.ts:503-511); `agent:spawnSession` has no cap and no sling/gt validation (agent-pane-ipc.ts:234-255). (4) **Resume**: the SDK conv session persists session.json and resumes (agent-pane-ipc.ts:48-54, session-store.ts:21-47); the pty route has no resume at all (claude-pty.ts:224-230). A THIRD bridge implementation also exists cross-layer: the engine's terminal.py serves the same claude-pty role for the legacy HTTP viewer (Pass 2 routing note, PROGRESS.md:57) and counts in the convergence decision.
- JUDGEMENT: Flipping the feature flag silently changes the security posture (skip-permissions vs approval prompts), the directory Claude operates in, resource limits, and continuity. For a daily-driver tool this is a behavioral fork, and every new agent feature must be built and debugged twice — the Settings toggle already missed one route.
- RECOMMENDATION: Decide the convergence target (the SDK route is the structurally better base — typed events, native permission hooks, no TUI timing races) and write a parity checklist (cwd, permissions param, worker cap, sling gating, resume) as bd issues; until the legacy pane retires, route both paths through one shared "session policy" module so defaults can't diverge. L overall; the cwd and skip-permissions parity items alone are S each.

### Session-registry failure modes: an errored SDK session becomes a message-eating zombie, and agent:send lazily creates a session for ANY sessionId [high / S]

- FACT: When `query()` throws, claude-backend.ts:173-185 only emits a `session`/`error` event — it does not call `queue.end()`, does not resolve pending approvals, and nothing removes the session from the registry (`sessions.delete` happens only in `endSession`, agent-pane-ipc.ts:66-71). `ensureSession` returns any existing map entry unconditionally (agent-pane-ipc.ts:41-43), and `send()` just pushes onto the now consumer-less queue (claude-backend.ts:190-193). In the other direction, `agent:send` calls `ensureSession({ sessionId: payload.sessionId, ... })` (agent-pane-ipc.ts:123-127) and `ensureSession` creates-if-missing for any id (agent-pane-ipc.ts:40-64) with no counterpart to the pty route's `MAX_WORKER_PTYS` check — a send to a stale or mistyped worker id (minted in the renderer as `worker-ctx-${Date.now()}`, renderer/toolbar.ts:342) silently starts a brand-new full Claude session, where the pty route treats "send to unknown worker" as a no-op (claude-pty.ts:658-661).
- JUDGEMENT: After any backend error (network drop, SDK crash), every subsequent `agent:send` to that sessionId is accepted and silently dropped — the worst failure mode for a chat UI — and pending `canUseTool` promises leak. Meanwhile a renderer routing bug turns into N silent background sessions (each a real `query()` subprocess/stream with cost) instead of an error.
- RECOMMENDATION: In the `run()` catch (and after normal loop exit), end the queue, deny/clear `pending`, and notify the host to drop the map entry (e.g. an `onClosed` callback symmetric to `onSessionId`) so the next send lazily creates a FRESH session. Make `agent:send` create-only-for-conv (worker creation exclusively via `agent:spawnSession`, sends to unknown ids a logged no-op) and add a session-count cap mirroring MAX_WORKER_PTYS. S.

### Priming is serialized twice in different layers with different output grammars, and triggered by magic wall-clock delays racing the claude TUI [med / M]

- FACT: The pty route serializes `ToolbarContextBundle` in MAIN — `bundleToPrimingText` + `buildCreateContextPriming` + `buildSlingPriming`, ~63 LOC emitting `# Context bundle` / `[Worker spawn — …]` shapes with β-marker instructions (claude-pty.ts:351-413). The agent route serializes the same bundle in the RENDERER — `bundleToPrompt` emitting a different `[Create Context]` / `Document:` shape with no marker grammar (renderer/toolbar.ts:214-237). Doc-switch priming is split the same way: agent route builds `[Now reviewing: …]` in main (agent-pane-ipc.ts:92-98) while the pty route writes `[Now viewing: …]` from the renderer (renderer/claude-pane.ts:17-19 module comment). On the pty route, delivery is timed, not observed: conversational priming fires at 1500ms then 350ms (claude-pty.ts:264-284); worker priming at 500ms then 400ms (claude-pty.ts:583-599); the 1500ms is itself a bump from a lost race — the in-code comment records that 500ms "races that clear and the slash-command line disappears from visible scrollback" and pre-plans the next fix, pinning behavior to "claude-code 2.1.146" (claude-pty.ts:258-263).
- JUDGEMENT: The rig-side priming language a worker receives differs by which UI flag is set, so prompt-contract fixes (truncation rules, β grammar) must be made twice and can drift, and there is no single seam to test priming text. The wall-clock tuples are machine-speed- and claude-version-dependent — a slow machine or a startup change silently drops skill activation. This whole fragility class disappears on the SDK route, which is itself an argument for the convergence finding above.
- RECOMMENDATION: Extract one pure, shared priming module (shared/, next to agent-pane/) that both routes call — bundle→text, doc-switch line, fresh-start line — with unit tests; on the pty route, trigger priming from observed output (first prompt-render data after spawn, or a quiet-period heuristic on `onData`) with the timeout kept only as fallback. M; the shared module is also the natural place to make priming anchor-kind-aware for the multi-format direction (capability note below).

### claude-pty.ts mixes six concerns and its pure parser core has zero tests [med / M]

- FACT: One 693-LOC file contains PATH binary discovery (`whichSync`, claude-pty.ts:89-105), the gt rig probe (:119-143), the TUI bracketed-paste protocol (:162-170), priming-text builders (:304-413), the β-marker line parser (`extractProgressMarkers`/`parseMarker`/`tokenizeMarker`, :419-488 — pure functions with no Electron/pty dependency), pty lifecycle for conv+workers, and IPC registration (:616-678). `find desktop/main -name '*.test.ts'` returns only agent-pane-ipc.test.ts and sidecar-migration.test.ts — no test touches claude-pty.ts, and the file itself notes its test seam has "no production caller" (claude-pty.ts:689-690).
- JUDGEMENT: The β-marker grammar is a hand-rolled protocol parsed by regex — exactly the code that breaks on edge input (chunk boundaries mid-line are handled, but quoted-escape and CR handling are untested). It is also the file's most reusable piece and the cheapest to harden.
- RECOMMENDATION: Split the pure parts (marker parser, priming builders, whichSync) into importable modules with vitest coverage using the existing adapter.test.ts fixture style; leave pty lifecycle + IPC in place. M.

**Capability note (JUDGEMENT, grounded):** the agent subsystem's context bundle is PDF-page-shaped, not anchor-shaped. FACT: `ToolbarContextBundle` hardcodes `currentPage`, `pageCount`, `selection.page` + xywh region, `nearbyComments[].page`, and its doc comment says "Source PDF absolute path" (shared/types.ts:852-882); the priming serializer renders `p.${s.page}` (claude-pty.ts:365) and the doc-switch line guesses format from file extension (agent-pane-ipc.ts:94-97). The agent pane is a main consumer of comments; as built it can only describe PDF-style locations to Claude — MD/DOCX/HTML comments fed through this bundle would carry meaningless page fields. When the unified model lands, version the bundle to carry the anchor union (or a pre-rendered location string per anchor kind) and derive doc-switch phrasing from the viewer's declared capability rather than extension sniffing — M, cheap if done with the shared priming module above.

**Also noted (cited):** `saveSessionId` writes with a direct `writeFileSync` (session-store.ts:43) — the one break in the repo-wide temp+rename discipline — and the module header still documents the file as living under "agent-viewer" userData, the app this code was ported from (session-store.ts:4-6; agent-pane-ipc.ts:3) (S). adapter.ts:3-4 claims "The adapter is stateless" while the same file defines per-session mutable `AdapterState` threaded through every call (adapter.ts:43-55; instantiated per session at claude-backend.ts:150) — one-line doc fix before someone shares one state across sessions and cross-wires streaming ids (S). `probeReviewer` caches forever with no production invalidation (claude-pty.ts:67-68, 119-120, 690-693) — installing gt mid-session requires an app restart (S). `ensureSession` silently ignores the `model` option for existing sessions (agent-pane-ipc.ts:41-43) — undocumented asymmetry with `agent:setModel` (S). `runOneShot` (claude-backend.ts:272-291) has no caller in-tree and its `AGENT_VIEWER_SMOKE=1` wiring doesn't exist in main/index.ts — delete or wire (S). Pass-3A flag, now CLOSED as a confirmed bug by the Pass-6 verifier: `registerAgentPaneIpc(mainWin)` binds the FIRST window while `app.on('activate')` creates fresh windows with no re-registration (main/index.ts:808-812) — agent-pane-ipc.ts holds a module-level `mainWindowRef` (agent-pane-ipc.ts:24, set :27-28) whose `isDestroyed()` guard (:114) silently drops every `agent:event` push to a re-created window, so after close-then-reactivate on macOS the agent pane never receives events (S — re-bind the ref on window creation). ✓ (C13, Pass-6 verified) POSITIVE: the agent IPC handlers are the only ones in the app doing runtime payload validation (typeof guards at agent-pane-ipc.ts:122, 144, 160-162, 202-209, 240-247) against the app-wide no-runtime-validation baseline, and agent-pane-ipc.test.ts (425 LOC) + adapter.test.ts (332 LOC, fixture-driven) are the strongest test cluster in the desktop layer — the SDK route is the right reuse substrate.

## Desktop Main — Persistence & Submit Pipeline

### Abandoned-round tombstone is re-emitted by the results watcher — the Abandon feature defeats itself [high / S]

- FACT: `abandonRound` renames a results file to the `.abandoned.json` soft tombstone — submit.ts:323 `const renamedTo = resolvedPath.replace(/\.json$/, '.abandoned.json');` (re-verified this session). The watcher's match regex still matches that tombstone name — results-watcher.ts:44 `const RESULTS_RE = /^results-.+\.json$/;` (re-verified; `results-x.abandoned.json` matches). Nothing in results-watcher.ts checks the `.abandoned` suffix (verbatim: `grep -rn 'abandoned' desktop/main desktop/renderer desktop/shared --include='*.ts'` → hits only in submit.ts, renderer/index.ts, shared/types.ts — zero in results-watcher.ts). The rename fires a watch event under the NEW name (handleFsEvent → `isResultsName(filename)`, results-watcher.ts:170-182) and `initialScan` re-emits every matching file on each doc open (results-watcher.ts:152-158). Completing the chain renderer-side (4C's file, cited only for the bug chain): `abandonInterruptedRound` deletes the in-memory round with the comment "The watcher will also fire for the rename, but we don't wait" (renderer/index.ts:2864-2868), and `applyResultsEvent` unconditionally re-adds any matching emit to `docState.rounds` (renderer/index.ts:2660-2667) with no abandoned check. ✓ (C9, Pass-6 verified incl. node-testing the regex against `results-x.abandoned.json`)
- JUDGEMENT: Clicking Abandon clears the round banner, then the debounced (120 ms, results-watcher.ts:46) re-emit of the renamed file resurrects the abandoned round — and it resurrects again on every subsequent doc open via the initial scan. The shared/types.ts:717-720 contract comment says "the rig's resume guard ignores the .abandoned suffix" — the app's own watcher is the one component that doesn't.
- RECOMMENDATION: Exclude tombstones in the watcher — an explicit `name.endsWith('.abandoned.json')` reject in `isResultsName` — plus a vitest case. S. If abandoned files should remain inspectable, emit them with an `abandoned:true` flag and let the renderer filter — but pick one side of the contract.

### Bundle writer is anchor-kind-blind — assumes PDF coordinates on every comment [med / M]

- FACT: `buildHighlight` reads `comment.anchor.region` with no discriminator check (bundle.ts:67-68, re-verified this session), and page grouping reads `c.anchor.page` (bundle.ts:203-204, re-verified). The data model forces this: `CommentPayload.anchor: AnchorRegion` is a REQUIRED, PDF-shaped field (shared/types.ts:130, AnchorRegion = page + x/y/w/h at types.ts:121-124) on every comment in every format, while md comments carry their real anchor in the optional `md_anchor` field (types.ts:157-165) keyed off a file-level `anchor_kind` discriminator (types.ts:168-170, 190). ✓ (C5, Pass-6 verified: zero anchor_kind/md_anchor checks anywhere in bundle.ts)
- JUDGEMENT: This is the live consumer of the type-model flaw flagged in the Process/IPC section — today only renderer call-site discipline prevents an md/html/docx comment (whose `anchor` is a placeholder) from flowing into `writeBundle` and producing a syntactically valid PDF with nonsense /Rect + /QuadPoints. Any DOCX comments.xml writer added beside this faces the same untyped seam.
- RECOMMENDATION: Make the anchor a discriminated union per comment (not a file-level flag plus parallel optional fields), and have `writeBundle` narrow to the pdf anchor kind, skipping/reporting others — the natural first brick of the unified model and per-format adapter layer. M (ripples through renderer + sidecars; schema_version bump or tolerant reader required).

### Atomic temp+rename write helper duplicated 5×, with 2 non-atomic outliers writing the same artifact [med / S]

- FACT: Byte-equivalent implementations of "tmp file + randomBytes suffix + rename + unlink-on-error" exist at submit.ts:73-83 (`atomicWriteJson`), bundle.ts:129-139 (`atomicWrite`), and inline in three main/index.ts handlers — `drafts:write` (491-505), `fs:writeFileText` (600-611, lane 3A's citation), `appState:write` (686-697). Two carry comments admitting the copy: submit.ts:71-72 "Matches the drafts / app-state pattern in main/index.ts"; bundle.ts:127-128 "Mirrors the drafts/app-state pattern in main/index.ts". Meanwhile two persistence writes skip the discipline entirely: the rename-recovery relink in `drafts:read` uses plain `writeFile` (main/index.ts:452) — write inside a read handler — and the migration rewrite uses plain `writeFile` (sidecar-migration.ts:92), both writing the same DraftsFile artifact that `drafts:write` protects atomically (per its own comment, main/index.ts:474-476).
- JUDGEMENT: A crash mid-relink or mid-migration can corrupt the very sidecar the atomic pattern exists to protect; five copies invite drift (one gaining fsync later while others don't).
- RECOMMENDATION: Extract one `atomicWriteJson(filePath, data)` into a small main-process fs util (or shared/), use it at all seven write sites. S.

### `gt mail` stdin EPIPE can crash the Electron main process [med / S]

- FACT: submit.ts:253-262 wraps `child.stdin.write(payload)` / `child.stdin.end()` in a synchronous try/catch; no error listener is ever attached to the stdin stream (verbatim: `grep -n 'stdin.on\|stdin\.' desktop/main/submit.ts` → only `254: child.stdin.write(payload);` and `255: child.stdin.end();`). The only error handler is on the child itself (`child.on('error', …)`, submit.ts:278), which covers spawn failure, not stream errors.
- JUDGEMENT: If `gt` exits before draining stdin (bad subcommand, version mismatch, immediate usage error), Node emits an asynchronous EPIPE on the stream; with no `'error'` listener that becomes an uncaught exception in the main process — the synchronous try/catch does not catch it. Low-frequency but worst-case blast radius (whole app), in the one pipeline that shells out per user action.
- RECOMMENDATION: Add `child.stdin.on('error', …)` routing into the existing `settle({ ok:false, reason:'gt_failed', … })` path before writing. S.

### Test investment is inverted: zero tests on the three highest-consequence persistence writers; the covered module is the one scheduled for deletion [med / M]

- FACT: Verbatim `ls desktop/main/*.test.ts` → `agent-pane-ipc.test.ts` and `sidecar-migration.test.ts` only. So submit.ts (334 LOC, spawns processes + freezes rounds), bundle.ts (302 LOC, writes PDFs into the user's source directory), and results-watcher.ts (318 LOC, debounce + cache + matching state machine) have no tests, while the one-time migration has 190 test LOC (sidecar-migration.test.ts:54-117). The ground-truth map lists this gap as known ("zero coverage on desktop viewers/submit/bundle") — this is assessment, not discovery. `buildHighlight`, `mintSubmitId`, `parseSourceName`/`buildBundleFilename` (shared/bundle.ts:22-57) and `isResultsName` are pure or near-pure.
- JUDGEMENT: The uncovered modules mutate user files and drive the round lifecycle, and two findings in this section (tombstone regex, anchor-blind quad geometry) are precisely the kind unit tests catch cheaply.
- RECOMMENDATION: Before any unified-model refactor, add vitest coverage for: RESULTS_RE/tombstone behavior, quad-point ordering + out-of-range page skip (bundle.ts:209-222), promote status-flip semantics (submit.ts:110-118), and shared/bundle.ts parsing/palette. M total; it de-risks every other recommendation in this section.

**Also noted (cited):** `mintSubmitId` (submit.ts:39-45) is byte-identical logic to `mintBundleId` (shared/bundle.ts:63-69) — `YYYYMMDD-HHmmss` UTC — and the frozen file path derives solely from it (submit.ts:92) with `atomicWriteJson` renaming over any existing file (submit.ts:78), so two promotes in the same UTC second silently overwrite a frozen round whose results pairing is keyed on `submit_id` alone (results-watcher.ts:123-124) — import a shared minting fn and uniquify (S). The one-time sha256→path sidecar migration (already completed per domain trap #5) is still permanent blocking startup machinery awaited before any IPC registration (main/index.ts:339-341), sha256-hashing full content of every .pdf/.md up to 10 000 entries when legacy sidecars exist (sidecar-migration.ts:130-138, 142), with a dead export (`legacyDraftsPathFor`: grep → single hit, the definition at main/index.ts:54), a forked hidden-dirs list (sidecar-migration.ts:109 vs main/index.ts:64-66), and a leaky hidden-FILE skip (sidecar-migration.ts:121-126) — time-box deletion, keeping `findSidecarByFingerprint` which rename-recovery still uses (main/index.ts:435) (S). The `drafts:read` ENOENT path re-implements first-500-chars fingerprint hashing inline (main/index.ts:432-439) instead of reusing `buildFingerprint` (sidecar-migration.ts:148-180), and `findSidecarByFingerprint` compares ONLY `first_500_chars_sha256` (sidecar-migration.ts:201-204), leaving `title_from_frontmatter` and `anchor_count` (shared/types.ts:175-180) computed-but-never-consulted — the unified model needs exactly one canonical doc-identity function (S). `buildSubject` takes a `sourceFileVersion` always passed `null` and never read (submit.ts:171, 177-180, 199) — dead parameter. results-watcher is a module-level singleton (`let active`, results-watcher.ts:66) — fine single-window, would silently steal the watch in a split view. Bundle PDF + JSON regenerated and written into the SOURCE directory on every save (bundle.ts:151-152, 176-177; overwrite-by-design per shared/bundle.ts:53-54) — by spec §10.4, but per-save full-PDF rewrite is the per-operation cost to watch as documents grow. The three-step submit (writeBundle → promoteDraft → slingViaGtMail) is three independent IPC calls with no rollback — a sling failure leaves the frozen submit file and flipped statuses behind; the orchestrating state machine is renderer/submit.ts → Pass 4C.

**Reusability for the multi-format direction (JUDGEMENT, grounded).** Format-AGNOSTIC already: `promoteDraft`/`slingViaGtMail`/`abandonRound` operate on `CommentPayload[]`, paths and ids only (submit.ts:85-151, 183-308); results-watcher matches on `submit_id` + `doc_version` strings (results-watcher.ts:123-124) — neither knows what a PDF is; the drafts/appState handler bodies are likewise path+JSON only. Format-SHARED single source of truth to keep: shared/bundle.ts palette + filename grammar + id minting (shared/bundle.ts:38-108), used by both processes by design (shared/bundle.ts:1-6). The per-format ADAPTER TEMPLATE: `writeBundle`'s pipeline — read source → group comments → write native annotations with minted ids → save → stamp ids back into a JSON sidecar → return id map (bundle.ts:141-156 step comment) — is exactly the shape a DOCX comments.xml writer needs; the blocker is the anchor-kind-blind data model (finding above), not the pipeline structure. NOT reusable: the migration walker's pdf/md-only doc discovery (`DOC_EXTS`, sidecar-migration.ts:110) — irrelevant once the migration is retired.

## Renderer — Orchestration & Chrome

**Pass 4 — key takeaways** (covers all three Renderer sections)

- Two silent data-loss bugs ship on the daily path: the first keystroke in any frontmatter'd .md permanently deletes its YAML block from disk (body-only `getContent()` written verbatim, md-viewer.ts:118-124/146-148 + renderer/index.ts:192-197), and re-opening a v1 doc after a completed round re-seeds the v1.1 drafts sidecar wholesale, destroying any comments added there since (renderer/index.ts:2954-2959, 2660-2667, 1261).
- The anchor-model flaw Pass 3 traced through types.ts and bundle.ts is now confirmed at every renderer layer: four real anchor shapes hide behind two declared kinds, html/docx anchors are smuggled through `md_anchor` with `as any`, and every non-PDF comment carries a placeholder PDF anchor that cards render as "p.1 · 0,0 0×0" and navigate with — the per-comment discriminated-union restructure is now evidenced end-to-end.
- The god-file's real cost is undeclared lifetimes, not length: four cloned doc-loaders whose hand-maintained dispose/reset lists have already diverged, 21 mutable module-scope `let`s mixing per-doc and per-app state, and an unguarded doc-open race that can persist doc A's sha256 under doc B's sidecar — `openDocument` + `DocSession` + a format registry is the structural precondition for the multi-format direction.
- Submit's unhappy paths are systemically broken: timeout Re-sling dead-ends on its own guards, Resume is a circular stub, Retry violates its own same-submit_id contract by minting duplicate rounds, sling failure strands comments as `submitted` forever, and the abandoned-tombstone chain is confirmed unfiltered at the renderer end too.
- On the new agent-pane route, Create Context / Sling workers are fire-and-forget — App.tsx drops every non-conversational event and no UI can see or stop a spawned session — so the feature-flag flip is not yet safe as a default; meanwhile zero tests cover submit, comment cards, and all four viewers, while the adjacent agent-pane code demonstrates the in-repo testing template.

---

### Concurrent doc opens race: docState is mutated across awaits with no epoch guard — can pair doc B's path with doc A's sha256 and persist A's comments under B's sidecar [high / S–M]

- FACT: `loadMarkdown` sets `docState.path = path` (renderer/index.ts:1398), then awaits `readPdfBytes` (1415), then sets `docState.sha256 = bytesResult.sha256` (1423) with no re-check that `docState.path` still equals its argument (all three lines re-verified verbatim this session). `loadPdf` has the same shape (1257, await at 1284-1287, sha write at 1300); its stale-doc check at 1311-1314 runs AFTER the sha assignment and its mismatch branch is an empty comment ("continue with viewer load below; … nothing to do here."), so a stale loader still calls `await h.viewer.loadBytes(bytesResult.bytes)` (1327) into the shared viewer. Only `loadDraftsForCurrentDoc` carries a real stale guard (`if (docState.path !== path || docState.sha256 !== sha256) return;`, 1817). All entry points funnel through `openFileFromTreeOrPalette` (608, 614, 630, 875) with no serialization, and the await window spans a full-file IPC byte read.
- JUDGEMENT: Two quick opens (tree double-click on different rows; palette Enter then another) interleave so the second loader resets `docState.path` to B while the first resumes and stamps A's hash; the next `flushDraftsWrite` (229-239, reads `docState.path`/`sha256`/`comments` at flush time) persists the mismatched pair — silent drafts mis-keying in exactly the data model the charter says matters most. The wikilink resolver compounds it: `resolveAndOpenWikilink` calls `openFileFromTreeOrPalette` for EVERY candidate that resolves (1791-1802), so an ambiguous link opens twice with a nondeterministic winner.
- RECOMMENDATION: Module-level load epoch (`const my = ++loadEpoch`, re-check after every await, abort on mismatch) or a single-flight open queue inside `openFileFromTreeOrPalette`; make loadPdf's decorative guard real in the same change. S–M.

### The doc-open pipeline is four cloned loaders (~450 LOC) whose reset/dispose lists have already diverged, atop 21 mutable module `let`s with undeclared lifetimes [high / M]

- FACT: `loadPdf` (renderer/index.ts:1230-1377), `loadMarkdown` (1379-1507), `loadHtml` (1509-1601), and `loadDocx` (1603-1681) each repeat the same sequence: drain md-save debounce, unsubscribe watcher, drain drafts debounce, a ~13-line manual docState/UI reset (1257-1277 vs 1398-1412 vs 1527-1542 vs 1614-1629), byte read, drafts load, viewer construction, an identical agent-notify tail with four copies of the inline `window as unknown as { agentViewer?: … }` cast (1344-1357, 1485-1503, 1579-1597, 1665-1677), then the same setActiveFile/appState/toolbar-event coda. The lists have already drifted: loadPdf disposes all three alt viewers (1240-1245); loadMarkdown disposes ONLY `mdViewerRef` (1426-1429) — html/docx viewer instances survive a switch to md; loadHtml disposes md+html but not docx (1519-1520); only loadDocx disposes all three (1609-1611). Selection caches likewise: `lastMdSelection` is reset only in loadMarkdown (1431), `lastHtmlSelection` only in loadHtml/loadDocx (1537, 1624), loadPdf resets neither (assignment-site grep: 172-173, 1431, 1442, 1537, 1560, 1624, 1646 — none inside loadPdf). The state these loaders must reset is module-flat: `grep -c -E '^let ' renderer/index.ts` → 21 (declarations at 167-179, 215, 272, 565-577, 973-977, 1157), plus mutable containers `docState` (131), `originRigPerDoc`/`recentRigsList`/`lastDestinationPerDoc` (147-149), `draftsCache` (217) — per-doc fields (selections, focusedCommentId, editingCommentId, lastBundleSnapshot) interleaved with per-app chrome (fileTree, palette, viewerHandlesRef, layoutWidths) with nothing marking which is which; 41 `addEventListener` and 94 `getElementById` call sites in the same file.
- JUDGEMENT: Every new format means a fifth clone and a fifth hand-copied reset list; the dispose and selection-cache asymmetries are drift already shipped (leaked viewer instances with live listeners, staleness masked only by per-format branching at 2084-2110 and 2159-2175). The god-file's real cost is that "what resets on doc switch" is a convention in the developer's head, not a datatype — no decomposition of index.ts can be verified safe until it is.
- RECOMMENDATION: Extract one `openDocument(path)` = `teardownCurrent()` + `resetDocSession()` + a per-format adapter `{ createViewer, load, postLoad }` selected from a registry keyed by `classifyPath`; introduce a `DocSession` object owning all per-doc fields (created/destroyed by openDocument) and a `ChromeState` for per-app fields. The four loaders shrink to adapter entries, and the registry is exactly the seam the multi-format capability direction needs. M.

### Per-format knowledge is scattered across ≥6 places in two processes with no registry; ViewerHandles is PDF-shaped for all four formats [med / S–M]

- FACT: Extension→kind logic exists as `classifyPath` (renderer/index.ts:549-556) and again as main's `classifyFile` (main/index.ts:68-75, Pass 3 citation); dispatch is if/else (768-780) with `other` falling into `loadPdf` (777-778); tree.ts hardcodes the openable 4-kind set twice (tree.ts:408, 416) plus a dimming check (376) under a stale comment claiming "PDF files are the only kind that round-trips to an opener for v1" (tree.ts:35-38); the current viewer is four parallel nullable globals (index.ts:168-171). `ViewerHandles` (79-90) bundles a `PdfViewer` plus prev/next/fit/dark buttons and is passed to all four loaders, three of which manually disable the page buttons (1476-1477, 1570-1571, 1656-1657). index.ts imports nothing from shared/file-viewer.ts (import block 1-56). The Open… button path (`handleOpenClick` → `openPdfDialog` → `loadPdf` directly, 1219-1228) is the only entry that bypasses kind dispatch entirely — Open… cannot open md/html/docx even though tree/palette can.
- JUDGEMENT: Adding one format (the charter's stated direction) currently touches classifyPath, the dispatch chain, a new viewer-ref global, tree.ts twice, main's classifyFile, and the pdf-only quick-open index (Pass 3 tail) — across two processes. This is the orchestration half of the seam whose viewer half is the decorative `FileViewer` interface (see "Renderer — Viewers & Anchoring").
- RECOMMENDATION: One `shared/file-kinds.ts` (extension map, openable set, per-kind capability flags such as paginated/editable/selectable) consumed by classifyPath, tree.ts, main's classifyFile, and the indexer; fold the per-format adapter registry of the previous finding on top; route Open… through the same dispatch. S–M.

### Skip-permissions setting is read through a window-global the new-agent-pane branch never wires — every app-state save silently rewrites it to `true` [med / S]

- FACT: `bootClaudeSettings()` is called only on the legacy branch of `bootClaudePane` (renderer/index.ts:449); the new-pane branch returns at 432 without calling it. The checkbox is exposed via a window global: `(window as any).__claudeSkipPermissions = () => skipCheck?.checked ?? true;` (487, re-verified verbatim). `flushAppStateSave` persists `claude_dangerous_skip_permissions: (window as any).__claudeSkipPermissions?.() ?? true` (804, re-verified verbatim) and is debounce-triggered by tree/layout/doc activity (e.g. 1372, 642, 651, 730). `grep -rn '__claudeSkipPermissions' desktop/` hits only renderer/index.ts:487 and :804.
- JUDGEMENT: With the new agent pane active the global is undefined, so every app-state write resets the persisted flag to `true`; a user who explicitly disabled `--dangerously-skip-permissions` (the toggle shipped in commit 8a98733) gets the dangerous default silently restored after flipping the pane flag and back. Mitigated only because `true` is also the field's default (read site 467) — explicit opt-outs are the harmed case. Root cause is a window global used as a cross-module settings API; this is also the third leg of the two-route permissions divergence (Pass 3's Claude/Agent finding: the SDK route ignores this setting entirely).
- RECOMMENDATION: Hold the setting in module state (seed from `readAppState`, update on the change listener) and have flushAppStateSave read that; make flushAppStateSave spread the previously-read state so fields with no live UI can never be clobbered to defaults. S.

**Also noted (cited):** `draftsCache` (renderer/index.ts:217) is path-keyed, never invalidated (set sites only — 238, 1831; no `.delete`/`.clear` in the file), ignores sha256 on hit and re-aliases the live comments array (1809-1815, 1812), and `seedNextVersionDraft` writes straight to disk without touching it (2959) — so reopening a previously-cached doc serves stale comments and can mask/overwrite the seeded v1.1 file (cross-ref the re-seed data-loss finding under "Comments, Submit UX & Panes"); key by path+sha256 or simply delete the cache (S). Three hand-rolled debounce instances — md save (176-206, 500 ms), drafts (215-256, 250 ms), app state (568-569, 782-810, 250 ms) — plus the "cancel timer then flush" drain re-inlined at eight sites (900-909, 917-927, 1168-1172, 1232-1236/1249-1253, 1383-1392, 1512-1516/1521-1525, 1606/1612); quit-flush correctness depends on every drain site knowing every timer (the md timer had to be retrofitted, comment at 898) — one `Debounced` utility + a registry the quit flush iterates (S). Every boot function silently no-ops when any of index.html's 87 element ids is missing (`grep -c 'id="' renderer/index.html` → 87; silent bails at index.ts:335-336, 410-412, 457, 512-515, 603-604, 991-995, 1989) — a typo'd id disables a whole subsystem with zero diagnostic; add a `requireEl(id)` that throws plus one jsdom smoke test (S). Per-operation cost: every debounced md save re-reads the WHOLE file over IPC just to recompute sha256 (`rehashCurrentDoc`, 208-212) and tree.ts's first filter keystroke stack-walks every directory under the root (tree.ts:267-270, 276-290), defeating its own lazy-load header rationale (tree.ts:3-5) — have `fs:writeFileText` return the new sha; debounce/cap the filter walk (S). Smaller, one line each: `basename` duplicated verbatim (index.ts:60-63 vs tree.ts:443-446; plus `dirnameOf` 544-547); the new-agent-pane localStorage flag is parsed in two modules (`useNewAgentPane` index.ts:38-44 vs `isNewAgentPaneActive` toolbar.ts:33-39) and the same buttons' disabled state and tooltips are split across them (toolbar.ts:197-201 vs index.ts:427-430); toolbar's `buildBundle` reads the required PDF anchor blind (`c.anchor.page`, toolbar.ts:243, 258) — one more live call site for the Pass-3 discriminated-union migration; engagement palette hex values duplicated between styles.css `:root` (13-15) and `ENGAGEMENT_PALETTE.cssHex` (shared/bundle.ts:94) with each side's comment pointing at the other as the source of truth (styles.css:11-12; bundle.ts:5-6); toolbar `flashErr` styles via six inline JS assignments (toolbar.ts:469-474), the lone outlier in otherwise CSS-disciplined chrome; `applyResultsEvent` re-tests `event.matchesDoc` already filtered upstream (index.ts:2629 vs 2618) — dead condition; init() boot order is load-bearing but uncommented (`restoreFromAppState` at 736 needs `viewerHandlesRef` set by `bootProjectOpenFlow` at 1016; order fixed at 311-322). POSITIVE: styles.css is 1891 LOC but consistently section-banner-commented with spec/bd references (e.g. 30-34, 343, 1050, 1771, 1820) and a single `:root` token block (1-16) — splitting is optional polish; vite-env.d.ts is a clean documented 13-line shim (vite-env.d.ts:3-9).

**Reusability for the multi-format direction (JUDGEMENT, grounded above).** The chrome modules are already format-agnostic and carry over unchanged: tree.ts has a documented host-callback contract and owns zero persistence (tree.ts:13-19); palette.ts is generic subsequence-match UI whose only PDF coupling is the `IndexedPdf` name and the pdf-only index upstream (palette.ts:13; the indexer filter is Pass 3's finding); splitter.ts is pure geometry (splitter.ts:33-68). What does NOT carry over is the orchestration core — the classifyPath if/else dispatch, four cloned loaders, PDF-shaped ViewerHandles, and four parallel viewer-ref globals must become the format registry + per-format adapter + DocSession of the findings above before DOCX/HTML comment work lands, or that work becomes the fifth and sixth clones.

## Renderer — Viewers & Anchoring

### Saving an edited Markdown file silently deletes its frontmatter — data loss in the app's only editable format [high / S]

- FACT: `MarkdownViewer.loadBytes` strips frontmatter before mounting the editor — `const { frontmatter, body, bodyOffset } = parseFrontmatter(text); … this.mountEditor(body);` (md-viewer.ts:118-124, re-verified verbatim this session) — and `getContent()` returns only the editor doc: `return this.editorView?.state.doc.toString() ?? '';` (md-viewer.ts:146-148, re-verified). The save path writes that body-only string straight to the source file: `flushMdSave` does `const content = mdViewerRef.getContent();` then `await window.electronAPI.writeFileText(docState.path, content)` (renderer/index.ts:192-197), debounced 500 ms from `onContentChange` (1437-1439) and flushed on blur (1452-1457). Nothing reattaches the stripped block: `grep -n 'frontmatter' renderer/index.ts` → no hits; the frontmatter is rendered into a DOM card only (`renderFrontmatter`, md-viewer.ts:122, 229) and `bodyOffset` is written and never read anywhere (`grep -rn 'bodyOffset'` → md-viewer.ts:95, :121 plus the parseFrontmatter internals — zero read sites). ✓ (C7, Pass-6 verified)
- JUDGEMENT: The first keystroke in any frontmatter'd .md permanently deletes its YAML block from disk — on the daily-driver path of the ONLY editable format. It also destroys the input to the sidecar's own rename recovery (`DocFingerprint.title_from_frontmatter`, shared/types.ts:176). Compounding: the frontmatter regex `/^---\s*\n([\s\S]*?)\n---\s*\n?/` (md-viewer.ts:283) lazily matches the first closing `---`, so an md file merely OPENING with a horizontal-rule-delimited block is silently eaten as "frontmatter" too.
- RECOMMENDATION: Store the raw frontmatter on the instance in `loadBytes` and prepend it in `getContent()` (or mount the full text and fold/decorate the frontmatter region — `bodyOffset` exists precisely for that translation and is currently dead). Add a vitest: load doc-with-frontmatter → edit → getContent contains the block. S.

### HTML/DOCX anchors are smuggled through `md_anchor` with `as any` behind a misreporting discriminator — the renderer half of the anchor-model flaw [high / M]

- FACT: `buildHtmlCommentPayload` persists a hybrid object into the typed `md_anchor` field with extension fields the schema doesn't have, cast away: `md_anchor: { char_start: sel.charOffset, …, quoted_text: sel.text, selector: sel.selector, char_offset: sel.charOffset, char_length: sel.charLength } as any` (renderer/index.ts:2214-2223; function at :2198), with degenerate `prefix: ''`/`suffix: ''` context and the same value duplicated under two names (`char_start` :2215 = `char_offset` :2221). Read-back sniffs untyped: `.filter((c) => c.md_anchor && (c.md_anchor as any).selector)` (1686, duplicated verbatim at 1702). The declared discriminator has only two values — `export type AnchorKind = 'pdf-glyph-rect' | 'md-fuzzy-snippet';` (shared/types.ts:170, re-verified verbatim) — and both iframe viewers report the md kind for selector anchors: `get anchorKind(): AnchorKind { return 'md-fuzzy-snippet'; }` (html-viewer.ts:55; docx-viewer.ts:41), while their actual payload is the CSS-selector `HtmlAnchor`/`HtmlSelection` shape (html-viewer.ts:4-16). Every html/docx comment also carries the required placeholder PDF anchor `anchor: { page: 1, region: { x: 0, y: 0, w: 0, h: 0 } }` (2204; md twin at 2237) — the exact value Pass 3 showed bundle.ts consumes blind. Separately, the md anchor shape is defined twice with no link: `MdAnchor` (shared/md/anchors.ts:1-7) vs the inline `md_anchor` literal (shared/types.ts:157-165). ✓ (C6, Pass-6 verified: the schema at types.ts:159-165 lacks the selector/char_offset/char_length extension fields)
- JUDGEMENT: There are FOUR real anchor shapes (pdf AnchorRegion, MdAnchor, HtmlAnchor, and the hybrid above) behind TWO declared kinds, one of which lies. Sidecars on disk now contain fields no schema describes; any consumer trusting `anchor_kind === 'md-fuzzy-snippet'` to mean MdAnchor (the engine, the rig, or `fuzzyMatchAnchor` itself) gets degenerate prefix/suffix anchors; and the zero-rect placeholder is one call-site mistake from a nonsense PDF Highlight. This is the live renderer-side proof for the discriminated-union restructure Pass 3 named the highest-leverage pre-work for the unified model; the payload-builder triplication that produces these objects is filed under "Comments, Submit UX & Panes".
- RECOMMENDATION: Add an `html-selector` kind to the per-comment discriminated anchor union when the Pass-3 restructure lands, make `FileViewer.anchorKind` report truthfully, delete the `as any` write/read sites, and make `MdAnchor` the single definition imported by types.ts. M (rides the already-recommended restructure; sidecar shim required).

### html-viewer and docx-viewer are ~60% verbatim twins that have already drifted, and their shared selector/charOffset capture scheme is lossy by construction [med / M]

- FACT: `cssPath` is byte-identical in both files (html-viewer.ts:172-195 vs docx-viewer.ts:177-200); `injectStyles` injects the same highlight/selection CSS (html-viewer.ts:91-107 vs docx-viewer.ts:101-114); `wireSelectionCapture` is the same mouseup→rAF→selector/charOffset capture (html-viewer.ts:109-138 vs docx-viewer.ts:116-141); constructor/stage/iframe setup is identical down to the docx viewer reusing the html viewer's CSS class names (docx-viewer.ts:21-26 vs html-viewer.ts:35-39); the host-side consumers are twins too (`applyDocxCommentHighlights` renderer/index.ts:1683-1697 vs `applyHtmlCommentHighlights` :1699-1713). Drift already shipped: on a failed text match, html-viewer's `findTextNode` falls back to the FIRST non-empty text node (html-viewer.ts:203-208 — predicate is `textContent.length > 0` despite the "partial match" comment) so `applyHighlights` highlights wrong text; docx-viewer's walker has no fallback and skips the anchor (docx-viewer.ts:157-171). The shared capture is structurally lossy: the offset is `indexOf` over the start element's text (`const charOffset = nodeText.indexOf(text);` then `Math.max(0, charOffset)`, html-viewer.ts:127-133; docx-viewer.ts:131-137) — first occurrence wins, multi-element selections yield −1 coerced to 0, and `container` is only `range.startContainer.parentElement` (html-viewer.ts:125; docx-viewer.ts:129). Resolution applies that element-relative offset to a single TEXT NODE (`range.setStart(textNode, start)…`, html-viewer.ts:157-164; docx-viewer.ts:161-168) — a different coordinate space whenever the element has multiple children — and `range.surroundContents(mark)` throws on partial non-text selections, swallowed by a bare `catch { /* skip silently (orphaned) */ }` (html-viewer.ts:164-167; docx-viewer.ts:166-172). No orphan state surfaces to the card UI, in contrast to the md path's explicit `orphaned` tracking (md-viewer.ts:33-57).
- JUDGEMENT: Every upcoming html/docx feature in the charter direction must currently be built and bug-fixed twice, and the fallback divergence already produces user-visible behavior differences (silently-wrong highlight vs silently-missing highlight). For exactly the selections reviewers make (multi-paragraph, spanning bold/links) the persisted anchor is wrong-by-construction with invisible failure. For the DOCX comments.xml round-trip these anchors are also the wrong substrate — `nth-of-type` paths against mammoth-regenerated DOM, with no stable mapping back to OOXML ranges.
- RECOMMENDATION: Extract one `IframeDocViewer` base (stage/iframe/sandbox, injectStyles, wireSelectionCapture, applyHighlights, cssPath, findTextNode) with a `bytesToHtml(bytes)` strategy — html = decode+base-href, docx = mammoth — collapsing the two index.ts highlight functions into one and picking ONE not-found behavior (skip, like docx). For capture, reuse the md strategy: prefix/quoted_text/suffix against the iframe's extracted linear text, resolved via `fuzzyMatchAnchor` + a text-node-walking Range, keeping the CSS selector only as a locality hint — designed inside the unified anchor union, not before it. M; do it before the docx/html comment-UI epics (rev-2h6, rev-6k6).

### The FileViewer interface is decorative: never used as a type by the host, PDF-shaped, and missing everything the viewers actually share [med / M]

- FACT: `grep -rn 'FileViewer' renderer/ shared/ --include='*.ts'` → outside its own file, every hit is an `implements` clause or type import in the four viewers (pdf-viewer.ts:78, md-viewer.ts:88, html-viewer.ts:24, docx-viewer.ts:11); the host never holds one — it keeps four parallel concrete refs (renderer/index.ts:168-171). The interface's surface (shared/file-viewer.ts:8-21) is paging/zoom — `nextPage/prevPage/fitPage/fitWidth` — which three of four viewers stub as empty no-ops (md-viewer.ts:126-129; html-viewer.ts:69-72; docx-viewer.ts:79-82), while everything the app actually needs polymorphically is absent: selection callbacks (four different shapes: `SelectionPayload` pdf-viewer.ts:54-62, `MdSelection` md-viewer.ts:17-21, `HtmlSelection` html-viewer.ts:11-16, an inline literal docx-viewer.ts:8), anchor highlighting (`revealAnchor` pdf-viewer.ts:288, `setTrackedAnchors` md-viewer.ts:161, `setHighlightedAnchors` html-viewer.ts:86/docx-viewer.ts:96), and load context (HtmlViewer alone needs `basePath`, html-viewer.ts:20). `FileViewerPageInfo` (shared/file-viewer.ts:3-6) has zero consumers and pdf-viewer re-declares it inline (pdf-viewer.ts:75). Selection-clear semantics diverge: pdf never emits a cleared event (early-return on collapse, pdf-viewer.ts:397); md/html/docx all emit `null` (md-viewer.ts:204-205; html-viewer.ts:115-117; docx-viewer.ts:122-125).
- JUDGEMENT: The abstraction exists but buys nothing — the host's per-format if/else dispatch (the Orchestration scatter finding) is forced partly because the interface can't carry the operations the host needs, so each new format adds a fourth nullable global and another dispatch arm. This is the viewer half of the registry seam the unified comment model has to land on.
- RECOMMENDATION: Renegotiate FileViewer around the anchor union: `loadBytes(bytes, ctx)`, `onSelection(unifiedSelection | null)`, `applyAnchors(Anchor[])`, `reveal(Anchor)`, plus a `capabilities` record (paged, editable, zoomable) so chrome enables controls by capability instead of format checks; host holds ONE `FileViewer` ref. M — pairs with (and partly pays for) the Orchestration registry work.

### Fuzzy re-anchoring persists unverified guesses back into the sidecar, destroying the user's original referent [med / S–M]

- FACT: `fuzzyMatchAnchor` steps 3-4 return matches WITHOUT verifying the text at the guessed position — the prefix path computes `expectedStart/expectedEnd` and returns `confidence: 'fuzzy'` with no comparison of `doc.slice(expectedStart, expectedEnd)` against `quoted_text` (shared/md/anchors.ts:54-63, suffix variant :65-74, 20-char partial :77-87) — and every step uses first-occurrence `doc.indexOf(...)` (:44, :55, :67, :79). The host then PERSISTS the guess: `reanchorMdComments` writes `c.md_anchor.char_start = match.from; c.md_anchor.char_end = match.to;` (renderer/index.ts:1758-1759), and on every content change `syncMdAnchorsToComments` rewrites the provenance fields from current positions — `c.md_anchor.quoted_text = doc.slice(a.from, a.to); c.md_anchor.prefix = doc.slice(Math.max(0, a.from - 40), a.from); …` (1775-1781; the `40` re-hardcodes `CONTEXT_CHARS` from anchors.ts:9). anchors.test.ts covers 5 happy paths only — no repeated-snippet, suffix-path, partial-path, or false-positive case (anchors.test.ts:31-74).
- JUDGEMENT: One wrong fuzzy hop is permanent — the next sync rewrites `quoted_text/prefix/suffix` to describe the WRONG location, so the reviewer's actual referent (what a rig redraft targets) is unrecoverable from the sidecar. This algorithm is also the designated reuse substrate for DOCX/HTML text anchoring (previous finding), so its robustness is leveraged.
- RECOMMENDATION: In steps 3-4, verify the candidate slice (similarity threshold; downgrade to `orphaned` on failure); prefer the occurrence nearest `char_start` when ambiguous; keep the ORIGINAL quoted_text/prefix/suffix immutable and write relocations to separate fields (the schema already has the `new_anchor` precedent, shared/types.ts:150-151); export CONTEXT_CHARS; extend anchors.test.ts with the four missing cases. S–M.

**Also noted (cited):** `PdfViewer.renderPage` is not serialized — concurrent calls share one canvas and mutable `this.page`/`this.viewport` with no in-flight guard or RenderTask cancellation (pdf-viewer.ts:166-243, render call :192-196; only the TEXT layer cancels, :248-265; direct callers :268-282, :335-341) — cancel the prior task at entry or chain through a `lastRender` promise (S). DOCX dark-mode semantics are inverted: the iframe document is hardcoded dark (`color: #e6e6e6; background: #222;`, docx-viewer.ts:59-60) while `setDarkMode` toggles the shared invert filter (docx-viewer.ts:84-87; styles.css:1828-1830) — dark-off renders dark, dark-on inverts to light; make the body theme-neutral in the IframeDocViewer extraction (S). md-viewer.ts:15 imports `type { MdAnchor }` that nothing in the file references — residue strict `noUnusedLocals` would catch if the first typecheck invocation weren't a no-op (Pass 3A tail). html-viewer's base-href fallback prepends `<head>` before any doctype when the doc lacks a head tag (html-viewer.ts:65) — malformed but browser-tolerated. A tracked md anchor orphaned by an edit stays orphaned even after undo restores the text (`if (a.orphaned) return a;`, md-viewer.ts:50-53) until a reload re-anchors. PdfViewer registers a document-level `mouseup` per instance (pdf-viewer.ts:141), removed only in `dispose` (:381) — correct today, a leak the moment a host forgets dispose (cross-ref the loaders' dispose drift in Orchestration). All four viewer classes have zero tests (known per ground-truth hygiene block — assessed, not discovered); the cheap pure seams are `mergeRectsByLine` (pdf-viewer.ts:486-506), `cssPath`, `parseFrontmatter`, and the fuzzyMatchAnchor edge cases above.

**Reusability for the multi-format direction (JUDGEMENT, grounded above).** The most reusable asset in this lane is `shared/md/anchors.ts`: pure, dependency-free, string-typed (anchors.ts:11-15, :34) and already vitest-covered — with the verification guard added it is the text-anchor leg of the unified model for MD, HTML, and DOCX body text alike. The PDF leg is equally ready: `SelectionPayload` capture (pdf-viewer.ts:394-445), `revealAnchor`'s PDF-point→viewport projection (:288-317), and the persistent highlight overlay (:447-463) are exactly the display/navigation primitives an Acrobat-annotation layer needs; because the viewer already consumes pdf.js's builder chain (TextLayerBuilder + StructTreeLayerBuilder, pdf-viewer.ts:31-34, :204-240), adding the missing sibling (pdf.js's annotation layer — Pass 5's question) slots into `renderPage` rather than requiring a rework. CodeMirror's `anchorField` position-mapping through edits (md-viewer.ts:42-57) is the right live-tracking mechanism — keep it, with the orphan-recovery and provenance fixes above. What does NOT carry forward: the selector/charOffset capture scheme (lossy by construction) and the two-copy iframe viewer pair — consolidate first, then build the html/docx comment UI once.

## Renderer — Comments, Submit UX & Panes

### Every submit failure path strands the user: recovery affordances are dead ends or violate their own contracts, and failed slings leave comments permanently `submitted` [high / M]

- FACT: Three recovery paths are wired but broken. (a) Timeout "Re-sling" is a guaranteed no-op: the banner button calls `retrySling()` (submit.ts:424), which dispatches `submit:retry-requested` (453-455); index.ts's only listener re-runs `handleSubmitBundle()` (renderer/index.ts:362-364), whose first guard flashes "Submit already in flight." (1093-1095) — and in the `timeout` state `canFire()` is false (`timeout` absent from submit.ts:716-721, re-verified verbatim) while `isInFlight()` is true (`timeout` included, submit.ts:136-140, re-verified verbatim). ✓ (C10, Pass-6 verified; one precision note: a doc-open check at index.ts:1086-1089 precedes the flash guard — doesn't break the no-op chain.) (b) "Resume round" is a circular stub: the interrupted-round banner's Resume only flashes "Press Cmd+Return to re-sling this round." (index.ts:2778-2783), but Cmd+Return → `handleSubmitBundle` is blocked by `hasInProgressRound()` (1097-1100; returns true for any `round_status === 'in_progress'`, 1143-1148, and "interrupted" is a subtype of in_progress, 2878-2887) — Resume tells the user to do the one thing the app refuses; the only working exit is Abandon. (c) Retry mints a new round while claiming it re-sends the same one: retrySling's comment says "Retry re-sends the same payload (same submit_id) so the rig sees a single round" (submit.ts:439-441; banner copy at :417), but the retry path re-executes promote (index.ts:362-364, 1105-1137; submit.ts:210-219) and `promoteDraft` mints a fresh `submitId` + fresh `submit-<id>.json` on every call (main/submit.ts:88-92); the event detail carrying `{submitId, destination}` (submit.ts:453-455) is ignored, so a no-origin user is also re-prompted by the destination picker (199-206). (d) The status flips are never compensated: `executeSubmit` dispatches `submit:comments-promoted` immediately after promote and BEFORE the sling (submit.ts:233-235); the listener applies open→submitted to live drafts and persists (index.ts:366-385); on sling failure the module transitions to `send_failed` (submit.ts:265, 268-308) with Retry/Dismiss only (380-405), Dismiss → idle (401), and no path anywhere flips `submitted` back to `open` (the only renderer-side status writes are the promote listener index.ts:376-378, rig-results `applyEntry` 2686-2692, and the seeder's fresh payloads 2947).
- JUDGEMENT: The happy path works; every unhappy path strands the user. Timeout recovery is dead, interrupted-round resume is circular, "retry" silently violates the rig-side dedup contract (results pairing is keyed on submit_id alone, results-watcher.ts:123-124 — Pass 3), and after a failed sling every comment shows `submitted` and is excluded from the next promote's flip set though the rig never received anything. The frozen submit-file-as-audit rationale (submit.ts:208-209) justifies keeping the file, not the live-draft flip.
- RECOMMENDATION: Give the submit module a real retry entry point — `retrySling(submitId, destination)` calling `submitSling` directly against the existing `promote.submitFilePath` (cache it next to `pendingSubmitId`), skipping promote and the picker; let `canFire()`/`handleSubmitBundle` accept the retry path in `timeout`/`send_failed`; wire Resume to the same entry point. Move the `submit:comments-promoted` dispatch to AFTER `slingResult.ok` (or dispatch a compensating un-promote on terminal failure/Dismiss). M — the state machine and IPC calls all exist; this is re-plumbing.

### Re-opening a doc with a completed round re-seeds the next version's draft, clobbering any comments added there since — latent data loss [high / S]

- FACT: `seedNextVersionDraft` writes the v1.1 drafts file wholesale — `const file: DraftsFile = { schema_version: 1, doc_version: newSha, comments: reraised }` then `writeDrafts(newDocId, newSha, file)` (renderer/index.ts:2954-2959, re-verified verbatim) — where `reraised` contains ONLY the deferred/needs-followup re-raises (2929-2952). The only guard is the in-memory `seeded` flag (2674-2679), which defaults to false whenever the round isn't already in the map (`seeded: prev?.seeded ?? false`, 2660-2667, re-verified verbatim) and lives in `docState.rounds`, rebuilt to `new Map()` on every doc load (1261, 1402, 1531, 1618). The results watcher re-emits every results file on each doc open (initial scan, results-watcher.ts:152-158 — Pass 3), so re-opening the v1 doc re-fires the complete-round event with `seeded` false and the seed re-runs. The in-code comment misjudges the consequence: "The existing draft would be overwritten cleanly (same sha256 path) but the dup work is wasted I/O" (2964-2966). ✓ (C8, Pass-6 verified)
- JUDGEMENT: Real workflow: round completes → user opens v1.1 and writes new comments → later re-opens v1 to compare → the seed re-fires and the v1.1 sidecar is atomically replaced with only the re-raises. The user's v1.1 comments are gone with no error, prompt, or backup — the drafts-sidecar equivalent of the engine's apply-before-validate corruption class. The stale `draftsCache` (Orchestration tail) can additionally mask the seeded file and let a later debounced flush overwrite it.
- RECOMMENDATION: Make seeding idempotent against DISK, not memory: before writing, `readDrafts(newDocId)` and skip (or merge re-raises into) a non-empty existing draft; alternatively persist a `seeded_from: <submit_id>` marker in the seeded DraftsFile and check it. S, testable without the rig.

### applyResultsEvent has no `.abandoned` filter — the renderer half of the tombstone-resurrection chain (closes the Pass-3 routing note) [high / S]

- FACT: `abandonInterruptedRound` renames the results file via main, deletes the in-memory round with the comment "The watcher will also fire for the rename, but we don't wait" (renderer/index.ts:2864-2868), and calls `resetSubmit()` (2869). `applyResultsEvent` unconditionally re-adds ANY matching emit to `docState.rounds` (2660-2667, re-verified verbatim) — `grep -n 'abandoned' renderer/index.ts` hits only the abandon-flow span (2852-2870), no filter in the event path. Combined with the watcher-side regex matching `.abandoned.json` names (results-watcher.ts:44 — Pass 3), the rename event re-inserts the abandoned round and `renderRoundBanner` prefers any `in_progress` round (2733-2736) — the banner resurrects.
- JUDGEMENT: This confirms and completes the bug chain Pass 3 routed here: neither layer filters tombstones today, so Abandon visibly defeats itself on the next event or doc open.
- RECOMMENDATION: Fix at the watcher (Pass-3 rec) AND add the belt-and-suspenders guard here: ignore events whose `filePath` ends `.abandoned.json` (or carry an `abandoned` flag on the event) in `applyResultsEvent`. S.

### On the new agent-pane route, Create Context / Sling workers are fire-and-forget: their events are dropped and no UI can see or stop them [high / M]

- FACT: With the new pane active, the toolbar spawns workers via `await (window as any).agentViewer?.spawnSession({ sessionId, prompt: agentPrompt });` (renderer/toolbar.ts:344, re-verified verbatim; sling twin at :393) with renderer-minted ids (`worker-ctx-${Date.now()}`, :342). The ONLY `onEvent` consumer in the renderer is App.tsx, which drops every non-conversational event: `const sessionId = event.sessionId ?? CONV_SESSION_ID; if (sessionId !== CONV_SESSION_ID) return;` (renderer/agent-pane/App.tsx:25-26, re-verified verbatim), with the in-code admission "worker session events are ignored here (they'd surface through dedicated worker UI in a future milestone…)" (App.tsx:20-22). Verbatim `grep -rn "onEvent" renderer/ --include='*.ts' --include='*.tsx'` → only ipc-client.ts (the wrapper) and App.tsx:24. The legacy route, by contrast, has full worker lifecycle UI — tabs, capped scrollback, progress strip, tasks panel with per-worker stop and retry (claude-pane.ts:589-651, 769, 846-1067) — none of which mounts when the flag is on (index.ts:414-425).
- JUDGEMENT: On the flagged route a real Claude session is spawned per click — uncapped, per Pass 3B's `agent:spawnSession` finding — whose output, errors, completion, and cost are all invisible and uninterruptible. A failed sling looks identical to a successful one. This is the renderer-side half of the two-route behavioral fork (Desktop Main — Claude/Agent) and the strongest single argument that the flag flip is not yet safe as a default.
- RECOMMENDATION: Before the new pane becomes the default: either route worker sessions to a minimal worker list (sessionId → status/stop, extending the store's event application — it is explicitly single-session today, store.ts:2-4), or keep Create Context/Sling on the pty route even when the conv pane is React (the toolbar already branches per feature). M.

### The comment surface is PDF-shaped end-to-end: triplicated payload builders with placeholder anchors, cards that render "p.1 · 0,0 0×0" for three of four formats, and reveal hard-wired to a never-nulled PdfViewer [med / M]

- FACT: `buildCommentPayload` / `buildMdCommentPayload` / `buildHtmlCommentPayload` (renderer/index.ts:2251-2271, 2227-2249, 2198-2225) are structurally identical ~20-line objects differing only in anchor fields; the md and html/docx builders fill the REQUIRED PDF anchor with `anchor: { page: 1, region: { x: 0, y: 0, w: 0, h: 0 } }` (2237, 2204), and the html/docx variant casts its hand-rolled blob into `md_anchor` `as any` (2223 — the anchor semantics are in "Viewers & Anchoring" above). The card renders that anchor as its breadcrumb — `· p.${revealAnchor.page} · ${x},${y} ${w}×${h}` (2427-2428) — and as its aria-label "on page N" (2399). Card click reveals via `viewerRef.revealAnchor(revealAnchor.page, revealAnchor.region)` (2401-2404) where `viewerRef` is typed `PdfViewer | null` (168), assigned once at boot (1008), and never nulled on doc switch (grep: only those two assignment sites — unlike the md/html/docx refs, disposed and nulled at 1240-1244, 1519-1520, 1609-1610). `handleSubmit` repeats the unshift→renderAllCards→scheduleDraftsWrite→clearInput→focus tail three times in per-format branches (2161-2196); the author is hardcoded `'AJB'` in all three builders plus both submit call sites (2210, 2243, 2266, 1135, 1184). Inconsistency bonus: stream-keyboard Enter reveals via `c.anchor` (2353) while click uses `c.new_anchor ?? c.anchor` (2398, 2403) — keyboard users get the stale pre-rig anchor.
- JUDGEMENT: The card stream — the single surface the unified cross-format comment model must render through — currently cannot describe or navigate to a non-PDF anchor at all: clicking an md/html/docx card "reveals" page 1 of whatever the persistent PDF viewer last showed, and three of four formats produce cards whose location line is a lie. Five copies of "what a comment is" plus an untyped anchor channel is exactly where the DOCX/HTML comment work will multiply bugs.
- RECOMMENDATION: When the discriminated anchor union lands (Pass-3 rec), collapse to one `buildPayload(buf, anchorUnion)` + a per-kind card location renderer (page+rect / char-range / selector), route reveal through the per-format viewer interface (`reveal(anchor)` on the active handle, nulled like the other refs), unify click/Enter on `new_anchor ?? anchor`, and extract a single `commitNewComment(payload)` tail + an `AUTHOR` constant (the tail extraction and honest typing of the selector shape are S now, pre-union). M overall — the natural companion to the shared/comments.ts restructure.

### Zero tests on the submit state machine and all comment-card/results logic, while the agent pane next door is the best-tested renderer code [med / M]

- FACT: `find renderer -name '*.test.ts' -o -name '*.test.tsx'` (run this session) → exactly `agent-pane/store.test.ts`, `agent-pane/timeline.test.ts`, `agent-pane/components/ContextMeter.test.ts`. Nothing tests submit.ts's 7-state machine (submit.ts:34-41), the picker option logic (pure list-building, 587-624), `applyResultsEvent`/`applyEntry`/`isLikelyInterrupted` (renderer/index.ts:2623-2711, 2878-2887), the seeder (2913-2969), or claude-pane.ts (1115 LOC, zero tests). The agent pane shows the codebase already knows how: `buildTimeline` was explicitly "extracted from the hook so it's unit-testable without React" (timeline.ts:25-28).
- JUDGEMENT: The recovery, re-seed, tombstone, and status-stranding findings in this section are all state-machine/event-application bugs of exactly the class cheap unit tests catch, and four of them sit on the round-lifecycle path that mutates user sidecars.
- RECOMMENDATION: Extract the transition table + guards (`canFire`/`isInFlight`/`markAcknowledged`/`markRoundComplete`) from DOM rendering in submit.ts (the agent-pane store/timeline split is the in-repo template) and vitest them; same for `applyEntry` + the seed-guard predicate. M — and the precondition for safely landing the retry re-plumb.

**Also noted (cited):** doc-switch during an awaited sling resurrects a ghost pill — `executeSubmit` awaits `submitSling` (submit.ts:242-252) then unconditionally transitions to `sent_unconfirmed` and starts the 10-minute timer (254-258), while every doc load calls `resetSubmit()` (index.ts:1267, 1404, 1533, 1620) which nulls `pendingSubmitId` (submit.ts:178-185), so `markAcknowledged` can never match (145-147) and a stuck pill/timeout lands on whatever doc is open — add a generation token checked after each await, the same pattern claude-pane already uses for pty data (claude-pane.ts:417-420) (S). The agent-pane zustand store persists the FULL transcript to localStorage on every streaming delta — `persist(…, { storage: createJSONStorage(() => localStorage) })` with messages/activities in the partialized slice (store.ts:100-101, 190-205), per-chunk merge on every event (76-98, 115-121), no cap or prune (only `resetTranscript`, 182-188) — O(transcript) stringify on the hottest UI path with a ~5 MB quota wall; cap or debounce (S). The dual-route doc-switch dispatch block is copy-pasted at all four doc-load sites with its own inline cast each time (index.ts:1340-1367, 1485-, 1579-, 1665-1677; grep `useNewAgentPane()` → 38, 414, 1340, 1485, 1579, 1665) — extract one `notifyPanesDocSwitch(payload)` (S; the cast itself is Pass 3A's stale-AgentViewerApi finding). claude-pane writes a cosmetic "[skill: /review-pdf-to-latex activated]" line to xterm on a bare 2000 ms timer (claude-pane.ts:405-412) — a visual claim of priming success independent of whether priming landed; a third magic delay in the wall-clock family Pass 3B flagged. Historical completed rounds re-toast "Round complete" on every doc open: `markRoundComplete` deliberately accepts calls when `pendingSubmitId` is null (submit.ts:158-167) and `applyResultsEvent` calls it for every re-emit including initial scans (index.ts:2629-2635) — gate on event source or a seen-set (S). `flashAnchorMeta` — a 2-second flash (index.ts:2559-2571) — is the SOLE error surface for submit preconditions, abandon failures, and seed failures (1087, 1094, 1098, 1116, 2861, 2921, 2961): failures vanish in 2 s with no log trail. The non-PDF submit gate is accidental: `handleSubmitBundle`'s message says "Open a PDF before submitting" but only checks path+sha256 (1086-1089), and `writeBundle` proceeds for an md/html/docx doc whenever the boot-time PdfViewer retains a stale nonzero `totalPages` (1162-1185 with viewerRef from 168/1008) — add an explicit `classifyPath(...) === 'pdf'` gate (S). `ChatComposer` hardcodes three model ids (MODEL_OPTIONS, ChatComposer.tsx:10-14) — single-source with the backend's model handling when the routes converge. `bindCommentStreamKeyboard`'s j/k handler and the picker's document-level keydown (submit.ts:543-555) are installed once and never disposed — consistent app-lifetime pattern, but fold into one listener registry when index.ts is decomposed.

**Reusability for the multi-format direction (JUDGEMENT, grounded above).** Format-AGNOSTIC already: the submit state machine + destination picker know nothing about PDFs — `SubmitContext` carries opaque bundle paths and `CommentPayload[]` (submit.ts:46-67), the pill/banner/picker operate on rig ids and states only, and round-banner/results application matches on `submit_id`/comment ids (renderer/index.ts:2641-2649) — all of it carries over to DOCX/HTML rounds untouched once "bundle" generalizes beyond a PDF pair. The agent-pane store/timeline/components are cleanly reusable (typed events, pure derivations, tested). The per-format ADAPTER GAP is concentrated in exactly three renderer seams, all cited above: payload construction, card location-rendering + reveal, and the bundle-write gate inside `writeBundle`/`handleSubmitBundle`. NOT reusable as-is: claude-pane.ts's worker UI is xterm-coupled — but it is the only worker-lifecycle UX in the app (stop/retry/progress, claude-pane.ts:846-1067); treat it as the requirements spec for the SDK-route worker surface, not as deletable legacy.

## Capability — Multi-Format Comment Round-Trip (special question)

**Pass 5 — key takeaways**

- **PDF round-trip splits cleanly across the two libraries already shipped**: READ + display native annotations via pdf.js `page.getAnnotations()` inside the existing `renderPage` (the Pass-4 routing slot), normalized into the unified model and rendered through the existing card stream + `revealAnchor`; WRITE/EDIT/DELETE via pdf-lib in main, extending bundle.ts. Both halves are proven in-repo (bundle.ts ships the writer; spike.mjs + readback.json prove read-back; pdf.js API docs confirm `getAnnotations`). Reject engine-extraction reuse for the live path.
- **DOCX: hand-rolled jszip/fflate + OOXML adapter in main, v1 scoped to flat comments; KEEP the SuperDoc rejection but on NEW grounds** — the 2026-05-16 format rationale is obsolete (DOCX is now in scope); the standing rationale is model-ownership + footprint vs the unified card stream. No JS library both reads and writes existing-file comments; python-docx 1.2.0 is a write-only hedge. Load-bearing spec nuance: comment CREATE/DELETE must insert/remove range markers in document.xml — "comments.xml only" is exactly true only for EDITS.
- **Unified model = per-comment discriminated anchor union (M-2) using W3C selector vocabulary, carrying three round-trip-driven fields prior passes didn't surface**: `quads[]` (not a single bbox), `origin` provenance (else round-trip imports duplicate on every bundle regeneration — "regenerated from scratch on every write", bundle.ts:152), and `native_ref` for edit/delete targeting. This is the precondition for both adapters and the direct continuation of the Pass-3/4 union findings.
- **The hardest non-code problem is vocabulary**: three status enums — engine 9 values (state.py:172-183), desktop 7 (types.ts:110-117), rig 5 (types.ts:230-236) — share zero strings (grep-proven) and have collision-prone near-synonyms (`needs_review` vs `needs-followup`). And the anchor union is a THREE-party schema event: `ResultEntry.new_anchor` is AnchorRegion-typed (types.ts:245), so the rig contract moves with the desktop schema, not after it.
- **The strongest existing asset is bundle.ts's pipeline shape** (read native → mint ids → write native → stamp ids into sidecar → return map, bundle.ts:141-156) — the DOCX writer should be a second instantiation of that exact shape; and the submit/results transport is already format-agnostic (matching is doc_version-string-only, results-watcher.ts:123-124).

### 1. PDF round-trip: pdf.js reads, pdf-lib writes — both already in package.json [recommendation; read+display S–M, write-back M]

- FACT (5A, spot-checked this session): The desktop PDF viewer has ZERO annotation-reading surface — imports are `TextLayerBuilder, StructTreeLayerBuilder` (pdf-viewer.ts:32-33), `renderPage` constructs `new TextLayerBuilder` (pdf-viewer.ts:204, re-opened), and `grep -n 'getAnnotations\|AnnotationLayer\|annotationStorage' desktop/renderer/pdf-viewer.ts` → no output, exit 1 (re-run this session). Native annotations are invisible in-app today.
- FACT: The WRITE half is production code: `buildHighlight` builds the full /Highlight dict (bundle.ts:59-108) with round-trip identity `NM: PDFString.of(annotationId)` (bundle.ts:104, re-opened); `appendAnnotsToPage` appends to an existing /Annots array (bundle.ts:112-125); the id is stamped back into the sidecar as `pdf_annotation_id` (types.ts:155-156, re-opened). Acknowledged v1 limit: "Single-region quad in v1 (renderer captures a bbox, not per-line rects)" (bundle.ts:73-76, re-opened).
- FACT: The READ half is spike-proven but unwired: `testReadback` walks `page.node.Annots()` for Highlight/StrikeOut/Underline/Squiggly/Text (spike.mjs:278-310, re-opened at :278) and its real output carries page/subtype/rect/contents/author/color/quads (desktop/spikes/rev-cvr-pdf-lib/readback.json:1-42, opened); StrikeOut and Text (sticky) dict shapes are spike-proven (2026-05-21 spike README); the pdf.js↔pdf-lib coordinate transform is the identity (README:26-28). Display primitives exist: `revealAnchor` projects PDF points → viewport (pdf-viewer.ts:288-317), `drawHighlight` merges per-line rects (:447-463).
- FACT (options evidence): pdf.js exposes `PDFPageProxy.getAnnotations()` and `PDFDocumentProxy.saveDocument()`/`annotationStorage` ("Storage for annotation data in forms") per official API docs (mozilla.github.io/pdf.js/api/draft/, fetched by 5A); pdfjs-dist ^5.7.284 and pdf-lib ^1.17.1 are both dependencies (desktop/package.json:71-72, re-opened). The engine path (PDF-C) is the wrong tool for a viewer read: `review-pdf extract` is a project BOOTSTRAP, not a query — requires `--pdf`, refuses if `.review-state/` exists without `--force` (cli.py:119, EXIT_EXISTING_STATE=3 cli.py:812, re-opened), renders every page to PNG as a side effect (extract.py:908), and its persisted `Annotation` is 8 fields with no subtype/quads/color//NM (state.py:192-199, re-opened; subtype read transiently at extract.py:321 then dropped — docstring gap acknowledged extract.py:211).
- JUDGEMENT (trade-offs): pdf.js-as-reader costs nothing new — the renderer already holds the `PDFDocumentProxy` (pdf-viewer.ts:160) and pdf.js does the parsing (UTF-16BE, inheritance, appearance streams) the spike does by hand; but its write path (annotationStorage) is form/editor-scoped with no home for engagement_level/redraft/status, so pdf.js-as-writer would create a second source of truth parallel to the drafts sidecar. pdf-lib-as-reader (PDF-B) keeps one library but re-parses the file in main and hand-writes every foreign-PDF case. Split them: read where the document is already parsed, write where the writer already lives.
- RECOMMENDATION: READ via `page.getAnnotations()` in `renderPage`, normalized immediately into the unified model and rendered through the EXISTING card stream — NOT a second pdf.js annotation DOM, so native and draft comments share one surface. WRITE/EDIT/DELETE via pdf-lib in main, extending bundle.ts into a `pdf-comments.ts` adapter: create = existing `buildHighlight` + spike's StrikeOut/Text shapes; edit = locate dict by /NM (app-written) or (page, annot index) captured at read time, writing /NM on first edit of foreign annots; delete = remove ref from /Annots. Two 1-day spikes before committing, not assumptions: (a) /IRT reply chains — PDF 1.7 §12.5.2 territory the spike did NOT exercise ("replies where feasible" = feasible-unproven); (b) per-line quads upgrade (the capture side already has `screenRects`, pdf-viewer.ts:60) so app highlights survive Acrobat round-trip without bbox-collapse. Port (don't invoke) the engine's two foreign-PDF heuristics when breadth lands: pdfplumber bbox-crop fallback (extract.py:113-156 region, domain trap #8) and sticky→highlight association (extract.py:176-243).

### 2. DOCX: jszip/fflate + hand-written OOXML adapter in main; SuperDoc stays rejected, on new grounds [recommendation; read M, write M, edit/delete S, display S]

- FACT (5A, spot-checked): The docx viewer drops comments at display time — `mammoth.convertToHtml` is called with `{ styleMap: ['u => em'] }` only (docx-viewer.ts:44-46, re-opened); nothing in the repo reads comments.xml (ground-truth matrix, confirmed). mammoth's README documents the `comment-reference` style mapping as the only comment affordance — display-only, no model API, no write (github.com/mwilliamson/mammoth.js, fetched by 5A).
- FACT (OOXML anatomy, per python-docx 1.2.0 user docs, fetched by 5A): comment content lives in `word/comments.xml`; the ANCHOR is range markers in document.xml, and ranges "must enclose _contiguous_ runs". CONSEQUENCE the charter wording glosses: CREATE/DELETE of an anchored comment must insert/remove range-marker elements in document.xml — body TEXT stays untouched but the part is mutated. The Phase-B spec must state this, not discover it. ✓ (C14, Pass-6 verified: all three quotes re-fetched verbatim from python-docx.readthedocs.io; the consequence is labeled inference)
- FACT (library options): JS `docx` (dolanmiu) is a generator ("Easily generate and modify .docx files… declarative API") with no documented read of existing-file comments — it cannot do the READ half. python-docx 1.2.0 has a real comments API (`document.comments`, `add_comment(runs=…, text=…, author=…)`) but documents two limits: no threading/resolution and no access to the comment-reference range — the anchored text (python-docx docs + release notes, fetched by 5A). So no library on the table does read+write of comments on an existing file in-process.
- FACT (SuperDoc): the 2026-05-16 rejection was decided on FILE-FORMAT grounds — "Our problem deals with two file types neither of which is DOCX"; disposition "Don't revisit unless the source-of-truth format changes from LaTeX to DOCX" (docs/research/2026-05-16-superdoc-fit-analysis.md). That ground no longer holds (DOCX is now a charter requirement). Current SuperDoc docs show a full comments module (addComment/addCommentReply/resolveComment, `documentMode: viewing`, DOCX export with `commentsType: "external"`) (docs.superdoc.dev/modules/comments, fetched by 5A); interop caveat on record: failed to display python-docx-authored comments until 2026 (superdoc issue #752, closed via PR #1505).
- JUDGEMENT: On raw DOCX-comment capability SuperDoc now EXCEEDS every other option (replies + resolution included), but it fails the architecture test: it is a ProseMirror+Yjs editor with its own comment store and sidebar UI, while the centerpiece requirement is ONE unified model rendered through the app's existing card stream (Pass 4C) with the drafts sidecar as store (Pass 3). Embedding it means a parallel comment store synced via events + export, its DOM/theming, and its OOXML interop history — for one of four formats. The hand-rolled adapter's real costs are namespace bookkeeping, parts/rels registration when no comments exist yet, run-splitting at mid-run anchors, and — the place DIY cost explodes — replies/resolution in commentsExtended. Scope v1 to flat comments, the same line python-docx itself drew.
- RECOMMENDATION: A main-process `docx-comments.ts` adapter on jszip or fflate (one new small dep; mammoth exposes no zip API — package.json:69), v1 flat comments: READ = parse comments.xml + locate range markers, emit unified comments with quoted_text from enclosed runs (M); WRITE = mint `w:id`, append to comments.xml (creating part + rels when absent), insert range markers with run-splitting, resolve anchors by TextQuoteSelector scan over run text (M); EDIT/DELETE = text swap / comment + marker removal (S, same module). Display: add mammoth's `comment-reference` style map so native comment positions show in the iframe (S, independent). Hedge: python-docx write-only via the engine if marker insertion proves nasty. Revisit trigger, recorded honestly: if requirements grow to threaded replies + resolution + tracked changes, hand-rolling commentsExtended is where DOCX-A's cost curve crosses SuperDoc's — redo this analysis then.

### 3. Unified comment model: discriminated anchor union (M-2) with W3C vocabulary, plus quads[]/origin/native_ref [recommendation; M for schema+migration+bundle retrofit]

- FACT (consolidating Passes 3–4, re-opened this session): today's shape is a REQUIRED PDF anchor on every comment (`anchor: AnchorRegion`, types.ts:130), optional bolt-on `md_anchor?` (types.ts:159-165, re-opened this pass — the field literal ends at :165), a FILE-level two-value discriminator (`AnchorKind = 'pdf-glyph-rect' | 'md-fuzzy-snippet'`, types.ts:170; on DraftsFile types.ts:190), html/docx anchors smuggled through `md_anchor` `as any` (Pass 4B, renderer/index.ts:2214-2223), and a writer that consumes the PDF anchor blind (bundle.ts:67, :203 — both re-opened this pass). Native round-trip identity exists for PDF only (`pdf_annotation_id`, types.ts:155-156).
- FACT (W3C reference): the Web Annotation Data Model's TextQuoteSelector (exact/prefix/suffix) + TextPositionSelector (start/end) pair is field-for-field isomorphic to the existing `md_anchor` (w3.org/TR/annotation-model/, fetched by 5A; types.ts:159-165). Full JSON-LD adoption is over-general for a single-user sidecar; the one pattern worth importing is selector redundancy ("Multiple Selectors SHOULD select the same content") — exactly the robustness fix Pass 4B demanded (immutable original quote, relocations stored separately).
- JUDGEMENT (options): M-1 (accrete `docx_anchor?`, `html_anchor?`, more file-level flags) is rejected on three passes' evidence — live bugs trace to exactly this pattern (bundle blind read, "p.1 · 0,0 0×0" cards, as-any smuggle); a fourth optional field scales the bug surface, not the capability. M-2 is the shape Pass 3 named "the highest-leverage pre-work" — and this pass adds three requirements the union must NOT ship without: (a) the PDF kind holds `quads: Rect[]` plural — native annots read back with per-line quads (readback.json) and the single-quad write is an acknowledged limit (bundle.ts:73-76); (b) per-comment `origin` provenance (`app-draft | native-pdf | native-docx | engine-extract`) so round-trip imports aren't re-written as duplicates on the next save — writeBundle regenerates the PDF from scratch every write (bundle.ts:152, re-opened) — and engine annotations.json rows (state.py:192-199) map in without schema change; (c) per-comment `native_ref` for edit/delete targeting (PDF /NM per bundle.ts:104 precedent; DOCX `w:id`).
- RECOMMENDATION: a `shared/comments.ts` v2 schema — per-comment `anchor: PdfQuadAnchor | TextQuoteAnchor | …` discriminated union (kinds named truthfully; html-selector demoted to a locality HINT per Pass 4B), plus `origin` and `native_ref`; DraftsFile `schema_version: 2` with a tolerant reader migrating v1 rows (file-level `anchor_kind` becomes derivable and is dropped); per-format adapters implementing one interface (read native → model / write model → native / capabilities record), with bundle.ts retrofitted as the first adapter. The text-anchor kind is ONE shape shared by md/html/docx. See the DRAFT sketch below for the field list.

### 4. Three status vocabularies, zero mapping code, near-miss names [high / M — spec work; code rides schema-v2]

- FACT (5B; counts corrected and grep re-run this session): engine `Status` is a 9-value Literal incl. `surfaced_pending`, `surfaced_resolved`, `needs_review`, `redrafted` (state.py:172-183, re-opened — 5B's lane file said "11-value"; the Literal contains exactly 9 strings). Desktop `CommentStatus` is 7 values incl. `needs-followup` and `build_failed` (types.ts:110-117, re-opened). Rig `ResultEntryStatus` is a 5-value subset (types.ts:230-236). Verbatim cross-check re-run this session: `grep -rn "surfaced_pending\|surfaced_resolved\|needs_review\|redrafted" desktop --include='*.ts' --include='*.tsx' | grep -v node_modules` → NO output (exit 1). Both worlds write under the same `.review-state/` dir (draftsPathFor main/index.ts:49; submit-<id>.json main/submit.ts:92) and near-miss names exist: `needs_review` vs `needs-followup` (underscore vs hyphen, different semantics); `deferred` defined separately in both.
- JUDGEMENT: The charter centerpiece is bridging exactly these two models, and the bridge is not merely missing — a naive merge silently conflates engine `needs_review` (apply-failure flow) with rig `needs-followup` (disposition). The unified spec must publish ONE status set with an explicit two-way mapping table BEFORE any adapter code is written; the draft table below marks the `?` cells that are owner decisions.

### 5. The rig results contract is PDF-typed too — the anchor union is a THREE-party schema event [med / S to spec; coordination is the real cost]

- FACT (5B, re-opened): `ResultEntry.new_anchor?: AnchorRegion | null` (types.ts:245) and `CommentPayload.new_anchor?: AnchorRegion | null` (types.ts:150) — both relocation fields are hard-typed to the PDF page+rect shape, written by the RIG ("Set by the rig when an `applied` redraft moved the underlying text", types.ts:147-149) and consumed by the renderer's reveal path (Pass 4C; `const revealAnchor = c.new_anchor ?? c.anchor;`, renderer/index.ts:2398). ✓ (C12, Pass-6 verified)
- JUDGEMENT: Every Pass-3/4 framing of the union was drafts-sidecar + renderer. This line means the union also changes what the rig must emit for relocated md/docx comments — the schema bump must be coordinated with the rig-side writer of results files, or relocations stay PDF-only. SubmitFile/ResultsFile already carry optional `schema_version?: 1` (types.ts:212, :258), so versioning is additive. This materially changes Phase-B sequencing versus the desktop-only framing.

### 6. The bundle grammar already parses md/tex sources but hard-commits every round to a PDF+JSON pair [med / S spec decision; writer generalization = the adapter work]

- FACT (5B, re-opened): `SOURCE_NAME_RE` accepts `(pdf|md|tex)` extensions (shared/bundle.ts:20), while `buildBundleFilename`'s contract states "bundles are always PDF + JSON pairs, even when the source is .md or .tex; v1 only ships PDF review so the .md/.tex case is moot here, but the grammar is the same" (shared/bundle.ts:32-37). Meanwhile `SubmitFile.bundle_pdf?`/`bundle_json?` are already OPTIONAL (types.ts:222-223) and the watcher's pairing never reads them — match is doc_version-only (results-watcher.ts:123-124).
- JUDGEMENT: MD rounds (sidecar-only) and DOCX rounds (native artifact = .docx with comments.xml) need "bundle" generalized to "per-format native artifact + JSON sidecar, both optional". The transport already tolerates this; only the grammar comment and the renderer/main writers assume PDF. Spec it as `{ native_artifact_path?, sidecar_json_path?, format }` per round; the §10.4 filename grammar carries over (it already versions md/tex names).

### 7. The complete native-annotation record exists in-repo only as throwaway spike output [med / S schema fields once schema-v2 lands]

- FACT (5B, re-opened): the spike's readback records carry `{page, subtype, rect, contents, author, color, quads[]}` (readback.json:1-42) — everything needed to repopulate a comment list except engagement level and redraft (spike README:141-148). The engine's persisted `Annotation` drops subtype entirely (state.py:192-199; transient read at extract.py:321, gap acknowledged in the docstring at extract.py:211); the desktop `CommentPayload` has the inverse gap — engagement/redraft/status but only the PDF-only `pdf_annotation_id?` as native link (types.ts:155-156).
- JUDGEMENT: The round-trip requirement needs ONE record joining native fields (subtype, native id, author, color, quads, reply parent) with app fields. Adopt the readback.json field set as the normative `native` block of the unified record, with `pdf_annotation_id` generalized to per-format `native_comment_id`. Engine-side, schema-v2 of annotations.json (persist subtype, native id, `in_reply_to`) is the unavoidable cost of making engine extraction a usable in-bound adapter (confirms Pass 2's reusability note).

### Reusable-asset inventory (5B, condensed; per-asset verdicts are JUDGEMENT on cited FACTs)

- **bundle.ts pipeline — THE adapter template**: `writeBundle`'s self-documented 6 steps (read source → mint `annot-<n>` ids → serialize+sha256+atomic write → sidecar with ids stamped back → atomic write → return id map, bundle.ts:141-156) with bidirectional PDF↔JSON linkage by design (/NM "so the JSON sidecar's per-comment link is resolvable from inside the PDF too", bundle.ts:56-58) and stateless one-line IPC (`bundle:write` → `writeBundleImpl`, main/index.ts:777-781). The DOCX writer is a second instantiation of this shape; the only blocker is the anchor-kind-blind input (Pass 3).
- **Engine status machine — most reusable engine asset**: the `(status, action)` legal-transition table (state.py:492-510) and id-addressed, format-free mutators (`set_annotation_status` apply.py:550, `record_proposal` :744, `append_chat_turn` :706, etc.) mention neither PDF nor LaTeX; adoptable as the canonical workflow substrate subject to finding 4's vocabulary mapping and the Pass-2 schema-guard fix.
- **Anchor legs**: `AnchorRegion` (types.ts:121-124) and the pure `MdAnchor` + `fuzzyMatchAnchor` module (shared/md/anchors.ts:1-7, :28-34 — no DOM, no Electron, string-in/string-out) are both real and reusable for any linear text incl. DOCX body and HTML extracted text, once Pass 4B's verification guard lands; everything AROUND them (required PDF anchor, file-level kind, smuggling) is what the union replaces.
- **Drafts sidecar**: path-keyed JSON that never touches the source file (draftsPathFor main/index.ts:47-50; atomic write :474-490; fingerprint rename-recovery types.ts:175-180) — right substrate for draft-stage comments in all four formats; known debts ride elsewhere (Pass 3C/4A).
- **Results transport — already format-agnostic**: promote/sling/watch/match is path/id/string-only (`matchesDoc = … submit.doc_version === state.sha256`, results-watcher.ts:123-124; replay sorted per §10.3, :149-156; promoteDraft/slingViaGtMail/abandonRound main/submit.ts:85/:183/:310). A DOCX or MD round needs zero transport changes; the two format leaks are findings 5 and 6.
- **Engine wrap-a-subcommand precedent**: `pdfHealth()` shells `runEngine(['pdf-health', …, '--json'])` with a documented exit-code set (engine.ts:264-288) — the template if the python-docx write-only hedge is ever needed.

### DRAFT — input to Phase B (evidence-shaped sketch, NOT a proposal; GATE 1 is CLOSED)

Assembled strictly from the cited assets above (5B Part 3); field names illustrative. It exists so Phase B starts from evidenced seams, not a blank page.

**Unified comment record (sketch):** `UnifiedComment { id; doc: {id, version, format: 'pdf'|'md'|'docx'|'html'}; anchor: Anchor; body: {comment, redraft?, redraft_suggestion?, engagement_level, author}; status: UnifiedStatus; native?: {comment_id /* generalizes pdf_annotation_id, types.ts:155-156; DOCX w:id */, subtype?, author?, color?, created?, in_reply_to? /* captured nowhere today */}; lineage?: {derived_from?, new_anchor?: Anchor /* re-typed per finding 5 */}; workflow?: {before_text?, proposed_text?, applied_text?, chat_log? /* engine AnnotationState, state.py:309-320 */} }` — with `Anchor = PdfQuadAnchor {page, region, quads?: Quad[]} | TextQuoteAnchor {char_start, char_end, prefix, suffix, quoted_text /* = MdAnchor verbatim, shared/md/anchors.ts:1-7 */} | HtmlSelectorHint {selector, char_offset, char_length, quoted_text /* legitimizes the smuggled hybrid as a declared kind, kept as locality hint per Pass 4B */} | DocxRange (placeholder — OOXML range vs text-fuzzy is the 5A §2 decision)`. Migration anchor (FACT): sidecars default to `'pdf-glyph-rect'` when `anchor_kind` is absent (types.ts:168-169 comment), and sidecar-migration.ts is the established tolerant-reader + one-shot migration precedent (Pass 3C).

**Per-format adapter table (DRAFT):** contract distilled from the two proven adapters — `read(native) → UnifiedComment[]` / `write(source, UnifiedComment[]) → {artifact, idMap}` / `anchorKinds` / `capabilities`:

| Format | In-bound read today | Out-bound write today | Anchor kind(s) | Gap to close |
|---|---|---|---|---|
| PDF | engine pdfannots (subtype read, NOT persisted); pdf-lib walk spike-proven | bundle.ts Highlight only; StrikeOut/Text proven unshipped | pdf-glyph-rect (+quads) | display annots in viewer; persist subtype/native-id/replies; StrikeOut/Text/reply write-back (§1) |
| MD | n/a — sidecar IS the store | sidecar only; no native format | text-fuzzy-snippet | verification guard + provenance immutability (Pass 4B); MD is the degenerate adapter |
| DOCX | none — comments.xml never read | none | docx-range and/or text-fuzzy (§2 decision) | the entire comments.xml adapter (§2); anchor↔OOXML-range mapping |
| HTML | none (no native comment format) | n/a — sidecar only | html-selector (hint) + text-fuzzy (truth) | declare the kind honestly (Pass 4B); no native round-trip required (charter) |

**Status-vocabulary mapping (DRAFT — the finding-4 table Phase B must finish):** unified `open/submitted/applied/rejected/deferred/needs-followup/build-failed/resolved` mapped against engine Status (state.py:172-183), desktop CommentStatus (types.ts:110-117), and rig ResultEntryStatus (types.ts:230-236); the load-bearing `?` cells are whether engine `needs_review` and/or `surfaced_pending` map to `needs-followup` (near-miss semantics, finding 4) and where engine `accepted`/`redrafted` fold into `applied` — owner decisions; no code against this table while GATE 1 is closed. Full table: evidence/pass5/5B.md §3.3.

**Also noted (cited):** the charter phrase "DOCX: comments.xml read/write, body read-only" should be amended in Phase B to acknowledge range-marker writes into document.xml (fact in §2). pdf.js's AnnotationEditorLayer as an in-place editing UX was deliberately NOT evaluated — its storage is form/editor-scoped per the official docs; card-driven editing needs nothing from it, and a visual-editing UX would be a separate Phase-B spike. The engine's bbox-crop fallback and sticky-association heuristics (extract.py:113-156 region, :176-243) are port candidates for the PDF adapter's foreign-file robustness. The spike dir (desktop/spikes/rev-cvr-pdf-lib/) was unowned by any lane until Pass 1 flagged it — now cited here as load-bearing evidence; `mintBundleId`/`mintSubmitId` duplication (shared/bundle.ts:63-69 vs main/submit.ts:39) was already filed by Pass 3C.

## Roadmap

_Phase B — requires GATE 1 open_

---

## GATE 1 Review Packet (Phase A complete — 2026-06-12)

### PRODUCED

- A 1-page **Executive Summary** (top of this document) with the Pass-6 verification stats (14/14 claims confirmed) and the five cross-cutting conclusions.
- Nine layer sections of cited findings — "Engine — Core Pipeline", "Engine — Server, Build & Terminal", "Engine — CLI, Tests & Cross-Cutting", "Desktop Main — Process, IPC & Engine Bridge", "Desktop Main — Claude/Agent Subsystem", "Desktop Main — Persistence & Submit Pipeline", "Renderer — Orchestration & Chrome", "Renderer — Viewers & Anchoring", "Renderer — Comments, Submit UX & Panes" — ~57 findings plus cited tails, each FACT/JUDGEMENT-separated and S/M/L-sized.
- The special-question section "Capability — Multi-Format Comment Round-Trip" — option recommendations (PDF split-library, DOCX hand-rolled adapter, M-2 anchor union) plus the labeled "DRAFT — input to Phase B" spec sketch, adapter table, and status-mapping skeleton.
- Independent verification of the 14 highest-impact claims, recorded in `evidence/pass6/verification.md` (verdicts, notes, tally); raw lane evidence in `evidence/pass1/`–`evidence/pass5/`.
- A complete pass log with per-pass anomalies and citation corrections in `PROGRESS.md`.

### UNCERTAIN

- **Sampling, not exhaustion**: 14 of ~57 findings were independently verified (all confirmed); the rest rest on lane citations plus per-pass synthesizer spot-checks. Three counting errors were caught and corrected during synthesis (PROGRESS.md Passes 1, 3, 5) — same-class residual errors may remain in unverified citations.
- **Web-sourced capability facts** (python-docx comments API, pdf.js `getAnnotations`/`annotationStorage`, mammoth comment-reference, SuperDoc comments module) were verified against live docs as of 2026-06-12; library versions drift.
- **Feasibility unproven, flagged as such**: PDF /IRT reply chains and per-line quads write-back (two 1-day spikes recommended in Capability §1); the DOCX anchor representation decision (OOXML range vs text-fuzzy, Capability §2/adapter table).
- **Owner decisions pending**: the status-vocabulary mapping `?` cells (engine `needs_review`/`surfaced_pending` vs desktop `needs-followup`; where `accepted`/`redrafted` fold — Capability §4, evidence/pass5/5B.md §3.3); keep-vs-kill for the legacy HTTP viewer + terminal.py; the convergence target for the two Claude routes; whether `_reviewer_rig_guard`'s commit-phase exemption is deliberate.
- The submit/round findings (C8/C9/C10) were verified against code, not by running the app against a live rig.

### CHECK BEFORE OPENING

- [ ] Read the Executive Summary and confirm the verification stats line (14/14) matches `evidence/pass6/verification.md`.
- [ ] Spot-check 2–3 high findings yourself by opening the cited lines (suggested: md frontmatter loss md-viewer.ts:118-124 + renderer/index.ts:192-197; re-seed clobber renderer/index.ts:2954-2959; commit-phase staging commit.py:307-327).
- [ ] Confirm you accept the M-2 discriminated anchor union as the Phase-B centerpiece, including its consequence: a THREE-party schema event (drafts sidecar + submit/results files + rig contract, types.ts:245) requiring rig-side coordination.
- [ ] Decide (or explicitly defer into Phase B) the owner-decision items listed under UNCERTAIN — especially the status-mapping `?` cells and the legacy-viewer keep-vs-kill appetite, since the roadmap shape depends on both.
- [ ] Confirm the five data-loss bugs (C1, C2, C7, C8, C9) should be sequenced as immediate fixes in the roadmap rather than folded into the larger refactors.
- [ ] Verify `GATES.md` still reads as you expect (no agent has edited it; both gates CLOSED as of this packet).

### RECOMMENDED DECISION

**OPEN GATE 1.** Verification came back clean (14/14, including the one previously-uncited link and the one long-open flag), the findings are evidence-dense and effort-sized, and the 5B draft gives Phase B a cited starting point rather than a blank page. There is no fix-first work that would change the roadmap's inputs — the urgent bugs are themselves roadmap items — and no lane gaps that would justify a re-run. The main caveat to carry into Phase B is the UNCERTAIN list above, especially the owner-decision cells.

### TO OPEN

Replace line 1 of `docs/reports/arch-review/2026-06-12/GATES.md` (human edit only — agents must never touch that file) with, filling in your name and the date:

```
GATE 1 (review findings → proposals & roadmap): OPEN — approved by <name> <YYYY-MM-DD>
```
